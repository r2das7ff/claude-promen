# Ночной автономный прогон — чеклист (PROM-EN каталог)

> Этот файл — задание для автономного запуска Claude Code (ночью, после сброса
> лимита токенов). Работай СТРОГО по чеклисту сверху вниз. После каждого пункта
> проверяй результат и отмечай `[x]`. Если прервёшься — повторный запуск
> продолжит с первого неотмеченного пункта.

## Контекст (прочитай сначала)
- Рабочая папка: `/Users/forgotaboutdre/Projects/TITAN GROUP/PROM-EN/site`
- Локальный сайт: WordPress+WooCommerce в Docker, http://localhost:8080
- Память проекта: `/Users/forgotaboutdre/.claude/projects/-Users-forgotaboutdre-Projects-TITAN-GROUP-PROM-EN-site/memory/` — прочитай MEMORY.md и все файлы.
- Плейбук категорий: `site/PLAYBOOK-CATEGORY.md` (как делать страницу категории).
- Тема: `site/wp-content/themes/promen/`. Шаблоны категорий: `woocommerce/taxonomy-product_cat-<slug>.php`. Карточка: `woocommerce/single-product.php` + `woocommerce/parts/`.

## ЖЁСТКИЕ ПРАВИЛА
1. Не ломать работающие страницы. После КАЖДОЙ правки PHP — `php -l <файл>`.
2. После правок CSS/JS — поднять `PROMEN_VERSION` в `functions.php` (сейчас 0.12.x → ставь следующий).
3. Docker-команды: `cd site && docker compose run --rm wpcli <cmd>`. Проверка страниц: `curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8080/<путь>"` (жди 200; проверь `grep -c "критическая ошибка"` = 0).
4. Контент — ТОЛЬКО из фактов: `content/aggregates.json`, `content/norm_aggregates.json`, тексты ГОСТ из `../normatives/pdf/<категория>/*.pdf` через `pdftotext -l 6 файл -`. НЕ выдумывать цифры/свойства.
5. Инженерный тон, без воды. Как на странице отводов (эталон).
6. Отмечай выполненные пункты `[x]` прямо в этом файле (Edit).

## ЧЕКЛИСТ

### A. Аудит всех шаблонов категорий (ГЛАВНОЕ — «очень важно»)
Категории верхнего уровня со своими шаблонами: sdt, otvody, troyniki, perekhody,
dnishcha, zaglushki, flancy, krepezh, bolty, gayki, shpilki, shayby, vinty,
tochenye, truby, izolyatsiya, opory, armatura.

- [ ] A1. Для КАЖДОГО шаблона выше открой страницу (curl + при возможности браузер) и проверь: (а) 10 секций + база знаний на месте; (б) НЕТ отвод-специфичного мусора у не-отводов (углы/радиус гиба/«крутоизогнутый», гнутая SVG-схема); (в) нормативы и марки стали соответствуют категории (сверь с `content/norm_aggregates.json` и `content/aggregates.json`); (г) счётчики в реестре = реальным (сумма = числу товаров категории); (д) нет плейсхолдеров/«загружаются следующим этапом». Записывай найденное в `site/AUDIT-REPORT.md`.
- [ ] A2. Карточка товара для КАЖДОЙ категории: открой 1 товар из каждой (SKU бери `docker compose run --rm wpcli eval` c WP_Query по слагу категории). Проверь: чертёж category-aware (для труб/фланцев/крепежа — схема сечения/нейтральная, НЕ гнутый отвод; поля «угол/радиус/плечо» отсутствуют у не-отводов — это уже сделано в `parts/blueprint.php`, но проверь фактически); конфигуратор свёрнут (≤12 строк + «показать все»); нет пустых секций с прочерками.
- [ ] A3. Все найденные в A1/A2 проблемы — ИСПРАВЬ. Правки блюпринта — только через `parts/blueprint.php` (общий). Правки данных категории — в её `taxonomy-*.php`. Отвод-копипаст в шаблонах не-отводов замени на корректный контент категории.

### B. Контент недостающих категорий
- [ ] B1. Заполни пустые описания терминов (term description) для верхнеуровневых: truby, opory, armatura, izolyatsiya, tochenye (сейчас пусто). 1–2 абзаца экспертного интро из фактов (норм, диапазоны, применение). Заливка: `wp term update product_cat <id> --description=...` через wpcli, ИЛИ через существующую команду `wp promen content` если поддерживает (см. scripts/promen-cli.php).
- [ ] B2. Подкатегории без своего лендинг-шаблона показывают РЕЕСТР (это норма): truby-bsh/es/ppu/vgp, opory-nepodv/skolz/pruzh, flancy-plosk/vorot/01/11, armatura-zadvizhki/klapany/krany. Проверь, что каждая открывается 200 и показывает список товаров (не белый экран). Если пользователь хочет для них полноценные лендинги — создай по PLAYBOOK-CATEGORY.md, копируя шаблон РОДИТЕЛЯ и адаптируя контент под подкатегорию (норматив, диапазоны из aggregates). Приоритет ниже, чем A и B1.

### C. Финал
- [ ] C1. Прогони по всем категориям и подкатегориям curl-проверку кодов 200 и отсутствия «критическая ошибка». Список слагов возьми из `promen_section_landing_slugs()` + подкатегории.
- [ ] C2. Заверши `site/AUDIT-REPORT.md`: что проверено, что найдено, что исправлено, что осталось. Кратко и по делу.
- [ ] C3. Обнови память: файл `catalog-full-and-filters.md` — допиши, что сделано ночью.

## Прогресс (лог)
- (пусто — заполняется ночным прогоном)
