# Каталог PROM-EN — чек-лист доработок

## Статус прогона A→E (2026-07-22)
- **A. Фильтры/поиск/сортировка — ГОТОВО**: счётчик раскапан (maxTotalHits), кликабельная
  сортировка DN/PN/Масса (сервер+AJAX), стабильные фасеты (дизъюнктивный универсум —
  опции не «скачут», 0-счёт серым), белый список углов, релевантность при поиске,
  глобальный поиск (scope=all + «искать во всём каталоге»), aria-live.
- **B. Фланцы — ГОТОВО**: в таблицу добавлены Dб (болт.окр.) и Болты (n×M); тип
  человекочитаемый (Воротник. 11 / Плоский 01…); карточка — единые подписанные строки
  геометрии + Тип. Пересобран индекс.
- **C. Правдивость — ГОТОВО**: гейт физ-валидности (s≥D/2 → пусто) на показе/индексации,
  0 невозможных; арматуре разрешён номинальный PN (карточка↔таблица согласованы).
  Пустые нормативы — не доходят до показа (резолвятся из title), C1 не требуется.
- **D. Каталог≠лендинги — ЧАСТИЧНО**: D5 терминология («лендинг»→«страница категории»)
  в комментариях; добавлен единый `promen_catalog_group_count()` (фундамент для counts).
  Осталось (крупный рефактор): D1 свести все счётчики к канону, D2 убрать хардкод-цифры
  со страниц категорий, D3 единый реестр категорий (≥4 хардкод-списка), D4 canonical done.
- **E. Гигиена — ЧАСТИЧНО**: E3 canonical группы→страница категории + индексируемость
  чистых категорий (готово). Осталось: E1 удалить мёртвые ветки (риск, отдельной задачей),
  E2 перф холодного пути, E4 throttle REST.

---


Единый список задач по итогам аудита (`CATALOG-AUDIT.md`) + свежие находки по
фильтрам/сортировке. Двигаемся сверху вниз по приоритету. Термин — **страница
категории каталога**, не «лендинг»; цель — единая реализация каталога.

Отмечать `[x]` по мере выполнения. После каждого пункта — живая проверка.

---

## A. Фильтры · Поиск · Сортировка (сейчас болит)

- [ ] **A1. Счётчик капается на «1 000».** Meili `maxTotalHits`=1000 → на крупных
  категориях (отводы 3423) фильтр применяется, но `#pCount` всегда «1 000 позиций» →
  выглядит как «фильтр не работает». Проверено: отводы `steel=20` list фильтруется, но
  count=1000; переходы `steel=20` count=378 (верно, <1000).
  **Фикс:** поднять `maxTotalHits` в настройке индекса Meili ИЛИ отдельный count-запрос;
  как минимум показывать «1000+». Файлы: `inc/catalog-search.php` (Meili params).
  *Приёмка:* отводы `steel=20` показывает реальные ~1917, меняется при фильтрации.

- [ ] **A2. Нет UI сортировки.** `sortControls:0, headerClickable:false` — кликабельной
  сортировки по колонкам (DN/масса/…) нет; есть только серверный дефолт `sort` в
  `catalog-schema.php`. Пользователь: «раньше было возможно».
  **Фикс:** кликабельные заголовки колонок (▲▼) → параметр `sort`/`dir` в
  `Promen_Catalog_Query` (движок уже умеет сортировать), swap через `catalog.js`.
  Файлы: `catalog-search.php` (принять sort из запроса), `archive-product.php`/`catalog-render.php`
  (заголовки-кнопки), `assets/js/catalog.js` (обработчик + URL-параметр).
  *Приёмка:* клик по «DN»/«Масса» меняет порядок, переживает AJAX и back/forward.

- [ ] **A3. Мультивыбор — подтвердить по всем категориям.** Механика работает
  (аккумуляция `steel=20,09g2s`, OR-union). Прогнать чек по всем группам + связки
  диапазон+чипы, чтобы точно нигде не ломалось.

- [ ] **A4. Поиск скоуплен категорией (аудит #1).** `q` всегда AND категория
  (`catalog-search.php:119-126,379-384`); живьё `q=17375`: global 1000 vs flancy 2.
  **Фикс:** режим «весь каталог» + переключатель; при 0 в группе — «N в других разделах».

- [ ] **A5. Релевантность выбрасывается (аудит #2).** Форс-сортировка dn/pn/mass/title
  (`catalog-search.php:250-253`). **Фикс:** при непустом `q` — relevance-порядок Meili.

- [ ] **A6. Whitelist углов на layer-фасетах (аудит #8).** `promen_valid_angles` не
  применяется в REST-фасетах (`catalog-api.php:106-107`). Применить.

- [ ] **A7. a11y фасетов (аудит #12).** `renderFilters` пересобирает innerHTML → потеря
  фокуса/`+ещё`; нет `aria-live`. Дебаунс/AbortController для range/поиска (аудит #18).

## B. Фланцы — привести к виду конкурентов (аудит #4, «хаос»)

- [ ] **B1. Колонки каталога фланцев.** Сейчас DN·PN·Тип·Dн·b·Масса; не выводятся
  `bolt_circle_d` (79%), `stud_count` (79%), `bolt_d` (62%). Целевой набор:
  **DN · PN · Тип · Dн · Dб · n×M · b · Масса**. Файл: `catalog-schema.php:51` (+ ребилд индекса!).
- [ ] **B2. Единая карточка фланца по всем нормативам.** Поля с прочерком, если нет:
  DN·PN·Dнар·Dболт.окр·dвн·n болтов·болт M·толщина b·ряд·уплотн.поверхность·тип·норматив·сталь·масса.
  `seal_face`/`series` сейчас только у ГОСТ 33259 → карточки разные. Файл: `single-product.php`.
- [ ] **B3. Человекочитаемый тип.** Коды `01/11/ФП/ФВ` → метки из `FLANGE_TYPES`
  (`promen-cli.php:45-50`). Убрать мёртвые/дубли PN (`dim_pn_mpa`, `dim_pn`).
- [ ] **B4. Дополнить `field_dictionary.csv`** фланцевыми `dim_*` (сейчас не документированы).

## C. Правдивость данных

- [ ] **C1. Пустые нормативы в БД (аудит #3).** Переходы 163/704, фланцы 41/822 → «—».
  Дорезолвить из title/full_designation (`resolve_normative_key` уже есть на импорте).
- [ ] **C2. Гейт физ-валидности при индексации (аудит #6).** Вернулись 19 невозможных
  (ОСТ 34.10.428 ×16). Пустить D/s при `s≥D/2` в `collect_dims`/`promen_build_catalog_document`.
- [ ] **C3. PN-раскол (аудит #15).** Разрешить номинальный PN арматуре (2.5/10 — реальный
  класс), а не только фланцам; свериться. Крепёж-масса скрыта везде (аудит #13), вычистить `_weight`.
- [ ] **C4. Заглушки dn=исполнение (аудит #14)** — 9 шт, дочистить (`sanitize_execution_as_dn`).

## D. Страница категории = каталог (единая реализация, НЕ лендинги)

- [ ] **D1. Убрать дрейф counts (аудит #5).** 3 источника: сайдбар (сумма детей),
  канонический store, `$term->count`; hero отводы **1000** vs сайдбар **3423**; страница
  фланцев хардкодит 866 поз vs live 822. **Все counts — из `promen_catalog_count`**.
- [ ] **D2. Убрать хардкод-HTML со страниц категорий.** `taxonomy-product_cat-*.php`
  хардкодят числа/DN/марки (`…flancy.php:83-189`) → дрейфуют. Данные секций — из
  канонического слоя/aggregates.
- [ ] **D3. Единый источник таксономии категорий (аудит #7).** Сейчас ≥4 хардкод-списка
  (`promen_section_landing_slugs`, `promen_catalog_group_views`, `promen_catalog_group_schema`,
  сайдбар `archive-product.php:16-102`). Свести к одному реестру категорий.
- [ ] **D4. Реестр(`?group=`) ↔ страница категории(ЧПУ) — согласовать.** Одна модель
  представления каталога, без параллельных путей; canonical группы → страница категории (аудит #10).
- [ ] **D5. Переименовать `catalog-schema.php`** (это схема КОЛОНОК, не schema.org) —
  снять именную коллизию (аудит #16). Термин «лендинг» вычистить из кода/доков.

## E. Гигиена / производительность / SEO / мёртвый код

- [ ] **E1. Удалить мёртвые ветки (аудит #7).** WP_Query-фильтры (`catalog-filters.php:238-369`),
  фолбэк-колонки `product-data.php:63-115`, `dim_pn_mpa`.
- [ ] **E2. Перф холодного пути (аудит #9).** Материализовать фасеты; лимит скоупа
  (`posts_per_page=>-1`). Кэш резолвера сегмента (аудит #11).
- [ ] **E3. SEO.** canonical группы → страница категории; noindex параметрических — ок;
  JSON-LD валидность; sitemap.
- [ ] **E4. Безопасность (аудит #19).** REST public без throttle — кэш/лимит на фасеты.
  (SQL параметризован, вывод экранирован — ОК, не трогать.)

---

## Порядок работ (рекомендуемый)
1. **A1–A3** (сортировка/счётчик/мультивыбор — прямая боль пользователя).
2. **B1–B3** (фланцы — «хаос»).
3. **C1–C2** (пустые нормативы + гейт валидности).
4. **A4–A5** (поиск: глобальный режим + релевантность).
5. **D1–D4** (единая реализация каталога, чистка дрейфа).
6. **E** (гигиена/перф/SEO).

*Каждый пункт: правка → `php -l` → при CSS/JS `PROMEN_VERSION++` → curl/браузер +
`wpcli` проверка → при смене колонок/схемы — ребилд индекса (`wp promen … rebuild`).*
