<?php
/**
 * Подбор изделия — /podbor/.
 * Та же панель, что и в плавающей кнопке, но встроенная в страницу:
 * скрипт видит #promenSelectorMount и строит разметку прямо в нём,
 * без оверлея (assets/js/selector.js, режим inline).
 */
// Модуль выключен — страницы не существует. Иначе осталась бы шапка с
// обещанием инструмента и пустым местом вместо него: стили и скрипт при
// выключенном PROMEN_SELECTOR не подключаются.
if ( ! promen_selector_enabled() ) {
	global $wp_query;
	$wp_query->set_404();
	status_header( 404 );
	nocache_headers();
	include get_query_template( '404' );
	return;
}

add_filter( 'promen_footer_idx', fn () => 'ПЭ-ПДБ / REV.1' );
add_filter( 'promen_strip_text', fn () => 'ПЭ-ПДБ' );

get_header();
?>
<div class="pg">

  <div class="psel-page-hero">
    <div class="psel-page-eyebrow">Инструменты снабженца · ПЭ-ПДБ</div>
    <h1 class="psel-page-h1">Подбор<br><em>изделия</em></h1>
    <p class="psel-page-desc"><b>Три шага.</b> Вставьте строку из спецификации или опишите задачу
      своими словами → при необходимости уточните условия среды → отметьте нужные позиции
      и отправьте запрос КП одним списком. Марки стали подбор проверит сам —
      по рабочей температуре и давлению.</p>
  </div>

  <div class="psel-page-tool">
    <div id="promenSelectorMount" data-reveal></div>
  </div>

  <div class="psel-page-seo" data-reveal>
    <h2>Как работает подбор</h2>
    <p>Подбор не «угадывает» изделие: он переводит ваши параметры в фильтры каталога
      и показывает только те позиции, которые есть в производственной программе PROM-EN.
      Типоразмер, норматив и масса каждой позиции — те же, что в её паспорте.</p>
    <p>Марки стали отбираются по справочнику материалов: для каждой марки известны рабочая
      температура и предельное давление по ГОСТ, и в подбор попадают только те, что проходят
      по заданным условиям среды. Марки, у которых справочник не нормирует нижний предел
      температуры, показаны отдельно — хладостойкость по ним подтверждается при согласовании.</p>
    <p>Результат подбора предварительный: окончательный выбор материала и исполнения
      инженер подтверждает при согласовании коммерческого предложения.</p>
  </div>

</div>
<?php
get_footer();
