# Яндекс.Директ: аудит текущих кампаний и план пересборки под новый сайт

Дата: 2026-09-03. Источник: Яндекс.Метрика, счётчик 62844301, период
2025-09-03 … 2026-09-03. Выгрузки: `perf-reports/ads/2026-09-03/`,
инструмент: `scripts/ads/direct_audit.py`.

**Как удалось достать расходы.** Рекламное пространство Метрики `ym:ad:*`
(клики, расход в рублях, конверсии) закрыто ошибкой `403 direct_client_logins`,
пока в запрос не передан логин аккаунта Директа. Логин виден в самом счётчике
(`owner_login` = `promenergetics`), после чего расход открывается полностью.
В `.env` он записан как `YANDEX_DIRECT_LOGIN`.

**API Директа открыт.** Одобренная заявка на доступ висит не на
`Promen-seo`, а на приложении `Cursor` — оно тоже наше, заведено под
агента Cursor (ClientID `9c8d7811…ffdb`, полный доступ, 03.06.2026).
Токен от него работает: `clients.get` возвращает ClientId 37164049 и лимит
1 100 000 units. Токен лежит в `.env` как `YANDEX_DIRECT_TOKEN`, выгрузка —
`scripts/ads/direct_api.py`. Имя приложения — только ярлык связки
«аккаунт + ClientID», так что второй заявки не нужно; важно лишь не
отзывать этому приложению доступ на oauth.yandex.ru — вместе с ним
умрёт и токен.

## 1. Деньги

| | За 12 месяцев |
|---|---|
| Расход | **671 337 ₽** |
| Клики | 38 205 |
| Визиты | 27 248 (недоезд 28,7%) |
| Средний CPC | 17,6 ₽ |
| Заявок через форму | 266 → **CPA 2 524 ₽** |
| Все целевые действия (форма, файл, email, телефон) | 471 → CPA 1 425 ₽ |
| Сделок из CRM | 0 |

### Поиск против сетей

| | Расход | Доля | Клики | CPC | Визиты | Заявки | CPA |
|---|---:|---:|---:|---:|---:|---:|---:|
| Поиск | 522 385 ₽ | 77,8% | 5 895 | 88,6 ₽ | 5 035 | 104 | **5 023 ₽** |
| Сети (РСЯ) | 148 952 ₽ | 22,2% | 32 310 | 4,6 ₽ | 22 213 | 162 | 919 ₽ |

Это переворачивает первое впечатление от одних только визитов: мусорный
трафик из мобильных игр виден невооружённым глазом, но стоит он копейки.
Деньги сгорели на поиске.

### Кампании по расходу

| Кампания | Расход | Доля | Клики | CPC | Заявки | CPA |
|---|---:|---:|---:|---:|---:|---:|
| Оптимизация ТГО — коммерческие запросы | 215 209 ₽ | 32,1% | 2 171 | 99,1 ₽ | 19 | **11 327 ₽** |
| Запорная арматура | 197 966 ₽ | 29,5% | 1 090 | **181,6 ₽** | 29 | 6 826 ₽ |
| Детали для АЭС / РСЯ | 93 081 ₽ | 13,9% | 24 856 | 3,7 ₽ | 108 | 862 ₽ |
| Ретаргетинг — РК от Яндекса | 55 871 ₽ | 8,3% | 7 454 | 7,5 ₽ | 54 | 1 035 ₽ |
| Стандарты АЭС и ТЭЦ (Россия) | 34 488 ₽ | 5,1% | 694 | 49,7 ₽ | 23 | 1 499 ₽ |
| Стандарты АЭС и ТЭЦ (ДНР/ЛНР) | 20 211 ₽ | 3,0% | 414 | 48,8 ₽ | 3 | 6 737 ₽ |
| Опоры трубопровода | 14 331 ₽ | 2,1% | 379 | 37,8 ₽ | 5 | 2 866 ₽ |
| Отводы крутоизогнутые | 8 703 ₽ | 1,3% | 231 | 37,7 ₽ | 6 | 1 451 ₽ |
| Тройники | 6 754 ₽ | 1,0% | 259 | 26,1 ₽ | 5 | 1 351 ₽ |
| Остальные 13 кампаний | 24 723 ₽ | 3,7% | 627 | 39,4 ₽ | 14 | 1 766 ₽ |

Две кампании — **61,6% годового бюджета (413 175 ₽)** при 48 заявках.

## 2. Настройки, которые всё это произвели

Данные из API Директа: 25 кампаний, 3 212 групп, 8 195 ключей,
3 231 объявление, 31 347 строк отчёта по поисковым запросам.

**Все 25 кампаний выключены** (17 в статусе OFF, 8 в архиве). Активных нет.
Все до одной — старого типа `TEXT_CAMPAIGN`; ни одной единой перфоманс-кампании,
на которую Директ переводит аккаунты с 2025 года.

### Стратегии — вот откуда CPC 88 ₽

| Стратегия | Кампаний | Что это значит |
|---|---:|---|
| `AVERAGE_CPC`, AverageCpc = **100 ₽**, лимит 3 000 ₽/нед | 19 | Директу прямо сказано платить в среднем 100 ₽ за клик |
| `HIGHEST_POSITION` (наивысшая доступная позиция) | 2 | Ставка не ограничена вообще. Это «Оптимизация ТГО» — 215 209 ₽ |
| `WB_MAXIMUM_CLICKS`, лимит 7 500 ₽/нед | 2 | «Запорная арматура» (198 000 ₽) и РСЯ «Детали для АЭС» |
| `WB_MAXIMUM_CONVERSION_RATE`, GoalId = 13 | 1 | Единственная конверсионная стратегия — и та обучалась на системной цели «вовлечённые сессии», а не на заявках |

Медиана ставки по 8 195 ключам — **85 ₽**, максимум 235 ₽. Средняя цена клика
в 100 ₽ для деталей трубопроводов задана вручную и ничем не обоснована.

### Флаги кампаний

| Настройка | Значение | Следствие |
|---|---|---|
| `CAMPAIGN_EXACT_PHRASE_MATCHING_ENABLED` | **NO во всех 25** | Точное соответствие фразам выключено — отсюда синонимы и автодобавленные фразы |
| `ENABLE_AREA_OF_INTEREST_TARGETING` | YES во всех 25 | Расширенный геотаргетинг: показы за пределами заданных регионов |
| `ADD_METRICA_TAG` | NO у 5 кампаний | Часть трафика уходила без разметки Метрики |
| `ALTERNATIVE_TEXTS_ENABLED` | YES у 19 | Директ сам переписывал тексты объявлений |

### Поисковые запросы: показы, CTR и цена

Отчёт покрывает 276 849 ₽ из 522 385 ₽ поискового расхода — детализация по
запросам хранится ограниченный срок. В этой части:

| | Показы | Клики | CTR | Расход | Доля | CPC |
|---|---:|---:|---:|---:|---:|---:|
| Всего | 44 409 | 2 652 | 5,97% | 276 849 ₽ | 100% | 104,4 ₽ |
| Синонимы (`SYNONYM`) | 16 356 | 964 | 5,89% | 138 833 ₽ | **50,1%** | **144,0 ₽** |
| Автотаргетинг (`NONE`) | 21 992 | 1 329 | 6,04% | 102 848 ₽ | 37,1% | 77,4 ₽ |
| Точное совпадение с ключом (`KEYWORD`) | 6 061 | 359 | 5,92% | 35 168 ₽ | 12,7% | 98,0 ₽ |

Половина поисковых денег ушла на синонимы по 144 ₽ за клик — прямое следствие
выключенного точного соответствия. Из 29 430 уникальных запросов
**28 834 не дали ни одной конверсии: 192 239 ₽, 69,4% расхода в отчёте**.

### Самые дорогие клики года

| Запрос | Показы | Клики | Расход | CPC |
|---|---:|---:|---:|---:|
| задвижки купить | 7 | 3 | 2 209 ₽ | **736 ₽** |
| задвижка клиновая стальная 30лс76нж ду 150 ру6 | 1 | 1 | 900 ₽ | 900 ₽ |
| задвижка abra а4016 080 с обрезиненным клином | 1 | 1 | 881 ₽ | 881 ₽ |
| задвижка кованая фланцевая ручная dn20 | 1 | 1 | 866 ₽ | 866 ₽ |
| производство задвижек из нержавейки | 1 | 1 | 848 ₽ | 848 ₽ |
| калькулятор металла | 448 | 16 | 1 095 ₽ | 68 ₽ |
| гост 10704 91 | 242 | 24 | 1 090 ₽ | 45 ₽ |

Аукцион по задвижкам — с профильными поставщиками арматуры, у которых это
основной ассортимент. У нас в каталоге 12 задвижек.

### Минус-слова: 310 штук не там, где нужно

Библиотека собрана: в крупных кампаниях 274–310 слов (`dwg`, `dxf`, `autocad`,
«своими руками», марки чужих брендов). Но именно те кластеры, что съели деньги,
не заминусованы:

| Слово | В скольких кампаниях из 25 |
|---|---:|
| «скачать» | 22 |
| «вес» | 22 |
| «бу» | 12 |
| «работа» | 3 |
| «гост» | 2 |
| «калькулятор» | 1 |
| **«прайс»** | **0** |
| **«теплица»** | **0** |

У кампаний «Ретаргетинг» и «Тройники» минус-слов нет вовсе.

### Посадочные в объявлениях

3 231 объявление, 107 уникальных адресов, все — старые URL:

| Ответ сервера | Объявлений | Адресов |
|---|---:|---:|
| 301 | 2 915 | 102 |
| 200 (главная) | 149 | 1 |
| 404 | 4 | 2 |

Здесь новость хорошая: редиректы в основном ведут по назначению — 1 141
объявление на `/catalog/sdt/troyniki/`, 786 на `/catalog/truby/`, 551 на
`/catalog/sdt/otvody/`. В 404 бьют всего 4 объявления
(`/zapornaya-armatura/` и `/otvody/svarnye-oss/ost-36-21-77/`). Проблема не в
массовых битых ссылках, а в том, что 54 объявления ведут в общую витрину
`/catalog/`, и ни одно — на калькуляторы, нормативную базу и проекты.

## 3. Диагноз

### Д-1. 98 000 ₽ в условиях показа, где ноль заявок уже что-то значит

**Как это считалось.** «Заявка» здесь — достижение цели Метрики
«Автоцель: отправка формы», атрибутированное условию показа по последнему
переходу из Директа. Ни продаж, ни сделок в этой цепочке нет: цель
`CRM: Заказ создан` пуста весь год, звонки не отслеживаются (4 клика по
телефону), письма напрямую на почту не видны. Так что «ноль заявок»
означает «Метрика не зафиксировала отправку формы», а не «не принесло денег».

Формально из 748 условий показа 709 не дали ни одной отправки формы, и через
них прошло 269 869 ₽ (40,2% бюджета). Но эта цифра обманчива — почти вся она
размазана по хвосту, где выводы делать не на чем:

| Объём условия | Условий | Расход | Что можно сказать |
|---|---:|---:|---|
| 1–4 клика | 577 | 73 036 ₽ | Ничего. При среднем CR 0,70% ноль заявок на четырёх кликах — норма |
| 5–19 кликов | 114 | 98 542 ₽ | Слабый сигнал, нужен ещё период наблюдения |
| 20–49 кликов | 12 | 33 654 ₽ | Уже показательно |
| 50+ кликов | 6 | 64 637 ₽ | Провал, сомнений нет |

**Твёрдый вывод даёт только нижняя часть таблицы: 18 условий, 98 291 ₽.**
Остальное — либо статистический шум, либо кандидаты на наблюдение, а не на
немедленное отключение.

| Условие | Кампания | Расход | Клики | CPC |
|---|---|---:|---:|---:|
| «Стоимость труб прайс» | Оптимизация ТГО | 30 857 ₽ | 267 | 115,6 ₽ |
| «Трубы цена прайс» | Оптимизация ТГО | 13 364 ₽ | 110 | 121,5 ₽ |
| Автотаргетинг | Запорная арматура | 10 296 ₽ | 48 | 214,5 ₽ |
| «задвижка стальная фланцевая» | Запорная арматура | 9 772 ₽ | 63 | 155,1 ₽ |
| «задвижки фланцы стальные» | Запорная арматура | 6 117 ₽ | 39 | 156,8 ₽ |
| «Прайс металлопрокат трубы» | Оптимизация ТГО | 5 457 ₽ | 51 | 107,0 ₽ |
| «задвижка стальная с выдвижным шпинделем» | Запорная арматура | 4 565 ₽ | 23 | 198,5 ₽ |
| «задвижки стальные цена» | Запорная арматура | 3 542 ₽ | 20 | 177,1 ₽ |
| «штуцеры трубопроводов» | Стандарты АЭС и ТЭЦ | 2 597 ₽ | 68 | 38,2 ₽ |
| Автотаргетинг | Отводы крутоизогнутые | 2 589 ₽ | 71 | 36,5 ₽ |
| ещё 8 условий | — | 9 135 ₽ | 230 | 20–100 ₽ |

Четыре прайсовых условия из этого списка — **51 769 ₽ и ни одной заявки**;
вели они на `/prajs-list-truby/`, страницу, которой больше нет.

### Д-2. Автодобавленные фразы дороже всего

| Тип условия | Расход | Доля | Клики | CPC | Заявки | CPA |
|---|---:|---:|---:|---:|---:|---:|
| Автоматически добавленные фразы | 280 487 ₽ | **41,8%** | 2 369 | **118,4 ₽** | 41 | 6 841 ₽ |
| Автотаргетинг | 240 962 ₽ | 35,9% | 22 437 | 10,7 ₽ | 133 | 1 812 ₽ |
| Условия ретаргетинга | 76 110 ₽ | 11,3% | 12 406 | 6,1 ₽ | 60 | 1 268 ₽ |
| Ключевые фразы | 73 430 ₽ | 10,9% | 984 | 74,6 ₽ | 32 | **2 295 ₽** |
| Фильтры по товарам | 348 ₽ | 0,1% | 9 | 38,7 ₽ | 0 | — |

Механизм автоматического расширения ключей забрал 41,8% бюджета по 118 ₽
за клик. Собственные ключевые фразы при вдвое меньшем CPC дали втрое
лучший CPA.

### Д-3. «Запорная арматура»: 198 000 ₽ под 12 задвижек, посадочная — 404

Кампания покупала «задвижки стальные» по 131–325 ₽ за клик. В каталоге
нового сайта задвижек — **12 позиций**, кранов 12, клапанов 9. Продать
столько арматуры, чтобы окупить CPC 200 ₽, нечем. Вдобавок посадочная
`/zapornaya-armatura/` после переезда отдаёт **404**, а редиректа на неё
в карте нет.

### Д-4. Треть оплаченных кликов не доехала до сайта

| Сегмент | Клики | Визиты | Потеряно | Оценка потерь |
|---|---:|---:|---:|---:|
| Поиск и главная Яндекса | 5 795 | 4 947 | 848 (14,6%) | ≈ 74 456 ₽ |
| Внешние DSP/SSP | 23 455 | 15 602 | 7 853 (33,5%) | ≈ 21 139 ₽ |
| Прочее РСЯ и сервисы | 2 846 | 2 090 | 756 (26,6%) | ≈ 14 320 ₽ |
| Мобильные приложения | 5 239 | 3 862 | 1 377 (26,3%) | ≈ 9 461 ₽ |

Суммарно **10 834 оплаченных клика без визита, ≈ 119 000 ₽**. В сетях это
нормальная механика случайных тапов, но 14,6% на поиске при CPC 88 ₽ —
это уже про скорость загрузки посадочных и качество мобильной версии
старого сайта.

### Д-5. Три четверти трафика — не покупатели, но стоят они мало

| Сегмент площадок | Расход | Доля | Клики | CPC | Визиты | Отказы | Заявки | CPA |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Поиск и главная Яндекса | 508 811 ₽ | 75,8% | 5 795 | 87,8 ₽ | 4 947 | 38,9% | 101 | 5 038 ₽ |
| Внешние DSP/SSP | 63 138 ₽ | 9,4% | 23 455 | 2,7 ₽ | 15 602 | 68,2% | 134 | 471 ₽ |
| Прочее РСЯ и сервисы | 53 910 ₽ | 8,0% | 2 846 | 18,9 ₽ | 2 090 | ~45% | 17 | 3 171 ₽ |
| Мобильные приложения (770 шт.) | 35 995 ₽ | 5,4% | 5 239 | 6,9 ₽ | 3 862 | 44,0% | 12 | 3 000 ₽ |

Формально у DSP лучший CPA в аккаунте — 471 ₽. Но это заявки с площадок
вроде `com.outfit7.mytalkingtom2` и `com.vitastudio.mahjong`, с глубиной
просмотра 1,00 и отказом 68%; планшеты дают 58,9% визитов. Прежде чем
масштабировать этот CPA, нужно поднять в CRM, во что превратились эти 134
заявки. Пока сделок из CRM ноль — считать их продажами нельзя.

### Д-6. Объявления ведут на страницы, которых больше нет

| Старый адрес | Визиты за год | Сейчас |
|---|---:|---|
| `/rubric-products/detali-dlya-tes-i-aes/#otvodygost17375` | 18 768 | 301 → `/catalog/` (витрина, якорь потерян) |
| `/prajs-list-truby/` | 561 | 301 → `/catalog/truby/` (прайса нет) |
| `/rubric-products/truby/` | 467 | 301 → `/catalog/truby/` |
| `/products/` | 445 | 301 → `/catalog/` |
| **`/zapornaya-armatura/`** | **291** | **404** |
| **`/izoljacija/`** | **29** | **404** |

19 217 визитов (70% рекламы) после переезда попадают в общий каталог
вместо профильной категории.

### Д-7. Рекламируется не то, что есть в каталоге

| Группа | Позиций | Доля SKU | Расход за год |
|---|---:|---:|---:|
| Крепёж (болты 4 598, шпильки 1 778, гайки, винты, шайбы) | 6 737 | 43,8% | **0 ₽** |
| СДТ (отводы, тройники, переходы, заглушки, днища) | 5 757 | 37,4% | основной объём |
| Трубы | 1 468 | 9,5% | ≈ 220 000 ₽ |
| Фланцы | 755 | 4,9% | **0 ₽** |
| Изоляция ППУ | 513 | 3,3% | **0 ₽** |
| Арматура | 33 | 0,2% | **197 966 ₽** |
| Опоры | 32 | 0,2% | 14 331 ₽ |

На 65 позиций арматуры и опор потрачено 212 000 ₽. На 7 492 позиции
крепежа и фланцев — ноль.

### Д-8. Поиск покупает справочные запросы

По реальным запросам пользователей (129 099 ₽ в разрезе фраз):

| Кластер запросов | Расход | Доля | CPC | Заявки |
|---|---:|---:|---:|---:|
| Номера ГОСТ / ОСТ / СТО | 36 624 ₽ | 28,4% | 44,1 ₽ | 12 |
| «купить», «заказать» | 16 742 ₽ | 13,0% | 137,2 ₽ | 5 |
| Прайс, цена, стоимость | 8 815 ₽ | 6,8% | 83,2 ₽ | 1 |
| Калькулятор, вес, таблица | 6 196 ₽ | 4,8% | 53,0 ₽ | 0 |
| Мусор (б/у, теплица, работа) | 669 ₽ | 0,5% | 51,5 ₽ | 0 |

Больше четверти расхода по фразам — номера стандартов. Это читатели
документации. На старом сайте им нечего было предложить; на новом
для них есть `/normativnaya-baza/`, но продавать им сразу нельзя.

### Д-9. Стратегиям не на чем учиться

`CRM: Заказ создан` — 0 достижений за год. Кликов по телефону — 4, то есть
звонки не считаются. Семь целей нового сайта (`request_any`, `request_kp`,
`request_tz`, `request_solution`, `request_docs`, `request_product`,
`request_calc`) заведены верно, код темы их шлёт
(`wp-content/themes/promen/assets/js/request-modal.js`), но статистики нет —
сайт запущен 1 сентября. Расход в сентябре нулевой: кампании стоят.

### Д-10. Помесячная динамика: CPA гулял в девять раз

| Месяц | Расход | Клики | CPC | Заявки | CPA |
|---|---:|---:|---:|---:|---:|
| сен 2025 | 59 624 ₽ | 677 | 88,1 ₽ | 13 | 4 586 ₽ |
| окт 2025 | 66 401 ₽ | 779 | 85,2 ₽ | 9 | 7 378 ₽ |
| ноя 2025 | 44 232 ₽ | 637 | 69,4 ₽ | 7 | 6 319 ₽ |
| дек 2025 | 73 193 ₽ | 3 228 | 22,7 ₽ | 9 | 8 133 ₽ |
| янв 2026 | 34 430 ₽ | 2 846 | 12,1 ₽ | 3 | **11 477 ₽** |
| фев 2026 | 58 440 ₽ | 8 059 | 7,3 ₽ | 44 | **1 328 ₽** |
| мар 2026 | 69 444 ₽ | 8 054 | 8,6 ₽ | 54 | 1 286 ₽ |
| апр 2026 | 48 001 ₽ | 4 692 | 10,2 ₽ | 36 | 1 333 ₽ |
| май 2026 | 62 385 ₽ | 3 458 | 18,0 ₽ | 37 | 1 686 ₽ |
| июн 2026 | 66 646 ₽ | 2 775 | 24,0 ₽ | 29 | 2 298 ₽ |
| июл 2026 | 65 531 ₽ | 2 247 | 29,2 ₽ | 18 | 3 641 ₽ |
| авг 2026 | 23 011 ₽ | 753 | 30,6 ₽ | 10 | 2 301 ₽ |
| сен 2026 | 0 ₽ | 0 | — | 0 | — |

Лучшие месяцы — февраль–апрель, когда основной объём давала дешёвая РСЯ.
Худший — январь: 34 430 ₽ на три заявки. Расход при этом почти не менялся
(44–73 тыс. ₽ в месяц), менялось только качество закупки.

## 4. Что даёт новый сайт

| Провал старых кампаний | Чем закрывается |
|---|---|
| «калькулятор металла», «вес трубы» — 6 196 ₽ и ноль заявок | `/kalkulyatory/` — вес СДТ, вес труб, фланцевый крепёж, метизы, ДН↔дюйм, аналоги сталей, доставка |
| номера ГОСТ — 36 624 ₽ на читателей документации | `/normativnaya-baza/`, карточки нормативов и серий |
| «подберите под задачу» некуда было принять | `/podbor/` |
| нечем подтвердить допуск к энергообъектам | `/proekty/` — Курская АЭС, Аккую, Руппур, Черепетская ГРЭС, ТЭЦ-3 |
| одна рубрика на весь ассортимент | 33 категории `/catalog/<группа>/<категория>/` с фасетами |
| прайс-лист как посадочная под «цену» | карточки с характеристиками и форма запроса КП по позиции |

## 5. Целевая структура кампаний

Семь сущностей вместо 21. Дробить при 266 заявках в год вредно:
каждая кампания должна набирать статистику, а не делить её.

**П-1. Поиск: СДТ** → `/catalog/sdt/otvody/`, `/troyniki/`, `/perekhody/`,
`/zaglushki/`, `/dnishcha/`. Группы по типу изделия × ГОСТ (17375, 17376,
17378, 17379). Посадочная — конечная категория, не витрина.

**П-2. Поиск: крепёж и фланцы** — 7 492 позиции, на которые не потрачено
ни рубля. `/catalog/krepezh/bolty/`, `/shpilki/`, `/catalog/flancy/*`.
Тексты по нормативу, а не по подписи в базе: 1 561 позиция названа
«болт фундаментный», хотя по ГОСТ это шпилька.

**П-3. Поиск: трубы и изоляция ППУ** → `/catalog/truby/*`,
`/catalog/izolyatsiya/*`. Прайсовые запросы («стоимость труб прайс»,
«трубы цена прайс») — в минус-слова: 50 000 ₽ и ноль заявок.

**П-4. Поиск: АЭС, ТЭС, нестандарт** → `/proekty/`, `/production/`.
Поставка на энергообъекты, изготовление по чертежу заказчика,
СТО ЦКТИ и ОСТ 108. Здесь продаётся допуск и опыт, а не деталь.

**П-5. Динамические объявления по сайту** — длинный хвост 15 407 карточек.
Товарная кампания и смарт-баннеры отпадают: каталог B2B без цен,
фид без цены под товарные форматы не принимается.

**П-6. Ретаргетинг по сегментам Метрики** — вместо «все, кто не
сконвертировался»: смотрел ≥2 карточек одной категории, открыл калькулятор
без заявки, скачал прайс, был на `/proekty/`. Свой баннер на сегмент.

**П-7. Лид-магниты** — калькуляторы и нормативная база. Цель —
микроконверсия и пополнение сегментов для П-6, а не прямая заявка.

### Что отключить сразу
- **Стратегию `HIGHEST_POSITION`** в «Оптимизации ТГО» — ставка ничем
  не ограничена: 215 209 ₽ и CPA 11 327 ₽.
- **`AVERAGE_CPC` со средней ценой 100 ₽** в 19 кампаниях — потолок ставить
  от фактического CPA, а не на глаз.
- **Включить точное соответствие фразам** — синонимы съели 138 833 ₽
  по 144 ₽ за клик.
- **Автоматически добавленные фразы** во всех кампаниях — 280 487 ₽ по 118 ₽ за клик.
- **Автотаргетинг в «Запорной арматуре»** — 10 296 ₽ по 214 ₽ за клик, ноль заявок.
- **Кампанию «Запорная арматура»** целиком, пока в каталоге 12 задвижек
  и посадочная отдаёт 404.
- **Показы в мобильных приложениях и внешние сети** — 99 133 ₽ трафика,
  качество которого не подтверждено ни одной сделкой в CRM.
- **709 условий показа без заявок** — начиная с прайсовых запросов по трубам.

### Минус-слова (стартовая библиотека)
бу, б/у, бесплатно, скачать, чертёж, реферат, википедия, что такое,
своими руками, вакансии, работа, авито, теплица, дача, прайс, прайс-лист.
Отдельно: «калькулятор», «вес», «таблица», «расшифровка» — минусуются
из коммерческих П-1…П-4 и ведутся в П-7 на `/kalkulyatory/`.

### Стратегии и цели
1. Недели 1–3: «Максимум кликов» с ограничением CPC — ставим потолок
   заметно ниже нынешних 88 ₽ на поиске; для автостратегии на конверсии
   нужно ≥10 целевых действий в неделю, сейчас их ~5 в месяц.
2. Недели 4–6: составная цель «Заявка: любая форма» + «по карточке товара»
   + «из калькулятора» → «Максимум конверсий» с недельным бюджетом.
3. С 7-й недели при 10+ конверсиях в неделю — оплата за конверсию.
4. Параллельно — передача сделок из CRM в `CRM: Заказ создан`.

### Ориентир по экономике
Резерв делится на три части разной надёжности:

| | Сумма | Насколько уверенно |
|---|---:|---|
| 18 условий с 20+ кликами и нулём заявок | 98 291 ₽ | Твёрдо: отключать |
| Две самые дорогие кампании (CPA 6 826 и 11 327 ₽) | 413 175 ₽ | Требуют не отключения, а нормальных ставок: сейчас там `HIGHEST_POSITION` и средний CPC 100 ₽ |
| Хвост из 691 мелкого условия | 171 578 ₽ | Статистики нет — наблюдать, не резать вслепую |

При сохранении годового бюджета и CPA на уровне лучших кампаний
(«Стандарты АЭС» 1 499 ₽, «Тройники» 1 351 ₽) те же деньги дают
не 266 заявок, а 350–450. Это ориентир, а не обещание: заявки —
это отправки формы в Метрике, их связь с продажами не прослеживается,
пока не подключены CRM и коллтрекинг.

## 6. Дорожная карта

**Сегодня — аварийно.**
Кампании стоят с 1 сентября, поэтому аварийность не в сливе, а в том,
чтобы не включить их как есть. Перед любым стартом: переписать посадочные
на новые адреса, снять `/zapornaya-armatura/` и `/izoljacija/` (404),
заменить `/rubric-products/detali-dlya-tes-i-aes/#otvodygost17375`
на профильные категории, проставить единую UTM-разметку.

**Неделя 1 — доступ к API Директа.**
Подать заявку на доступ приложения `Promen-seo` к API Директа (см. раздел 6),
дождаться подтверждения. После этого — выгрузка минус-слов, ставок,
стратегий и статусов; сверка с этим отчётом. До подтверждения работать
через веб-интерфейс и Коммандер.

**Недели 2–3 — пересборка.**
Собрать П-1…П-4 на новых посадочных, объявления под конечные категории
(ГОСТ, Ду и Ру, марка стали в тексте), быстрые ссылки на калькуляторы
и нормативы. Запустить крепёж и фланцы. Залить минус-слова и чёрный
список площадок.

**Недели 4–6 — накопление и перевод на конверсии.**
Статистика по новым целям, составная цель, перевод П-1…П-3 на
«Максимум конверсий». Запуск П-6 на накопленных сегментах.

**Недели 7+ — масштабирование.**
П-5, расширение географии по факту заявок, а не по цене показа.
Еженедельный отчёт: `python scripts/ads/direct_audit.py --days 7`.

## 7. Доступы

| Что | Статус |
|---|---|
| Метрика: визиты, поведение, цели | **есть** |
| Метрика: расход, клики, CPA по кампаниям | **есть** (через `direct_client_logins=promenergetics`) |
| Вебмастер | **есть** |
| API Директа: минус-слова, ставки, стратегии, статусы | **есть** (токен своего приложения `Cursor`) |
| Roistat: расходы и сделки | нет доступа |
| Коллтрекинг | есть UIS, но звонки не передаются в Метрику |
| Офлайн-конверсии из CRM | не передаются (цель пуста весь год) |

### Про доступ к API
Работаем токеном приложения `Cursor` — заявка на доступ к API одобрена
именно на нём. Токен от `Promen-seo` получает ошибку 58 «Незавершенная
регистрация»: право `direct:api` у приложения есть, но заявки на него нет.
Подавать вторую заявку смысла нет — доступ выдаётся связке «аккаунт +
ClientID», а оба приложения наши. Что важно помнить: не отзывать доступ
приложению `Cursor` на oauth.yandex.ru и обновить токен, когда истечёт
срок его жизни (у Яндекса это обычно год).

### Wordstat
Частотность запросов теперь берётся не из Директа, а через Yandex Cloud
Search API в [Yandex AI Studio](https://aistudio.yandex.ru/docs/ru/search-api/operations/wordstat-getdynamics.html):
сервисный аккаунт с ролью `search-api.webSearch.user` и API-ключ со
scope `yc.search-api.execute`. Это отдельный ключ, к OAuth-токену Яндекса
отношения не имеет. Нужен для сбора семантики под П-1…П-4.

## 8. Сквозная аналитика: от клика до сделки

Состояние на 04.09.2026. Задача — чтобы Директ оптимизировался не на
отправку формы (в которую попадают спам и случайные тапы из мобильных
игр), а на квалифицированную заявку, подтверждённую менеджером.

### Готово

**Идентификаторы визита в заявке.** `request-modal.js` забирает ClientID
у самой Метрики (`getClientID`) и `yclid` из адреса первого захода с
Директа; yclid кладётся в cookie на 90 дней, потому что заявку человек
оставляет позже и с другой страницы. Оба значения уходят и из модалки
(FormData), и из формы секции 11 (скрытые поля), а `promen-requests.php`
пишет их в мету заявки: `_promen_ym_client_id`, `_promen_yclid`.
Проверено на стенде: значения доезжают и сохраняются.

**Приёмник в Метрике.** Создана цель «CRM: квалифицированная заявка»
(id 608772488, идентификатор события `crm_qualified`). Окно приёма
офлайн-конверсий расширено с 21 до 90 дней — для B2B-цикла это
обязательно, иначе сделки приезжают позже, чем Метрика готова их принять.

**Загрузчик.** `scripts/ads/offline_conversions.py` принимает CSV со
сделками и льёт в Метрику двумя пачками: по `yclid` (точнее — конверсия
ложится ровно на кампанию и фразу, приведшие человека) и по `ClientID`
для остальных. Есть `--dry-run` и просмотр статуса загрузок.

### Как устроена CRM (снято 04.09.2026 через MCP)

Портал `anisimovgroup1.bitrix24.ru`. **Четыре воронки сделок, разведённые
по производственным площадкам**, а не по типам сделок:

| Воронка | Стадий | Схема |
|---|---:|---|
| `[0]` Общая | 13 | КП/СЧЁТ → ДОГОВОР → ОПЛАТА → СБОР СЧЕТОВ ОТ ПОСТАВЩИКОВ → СЧЕТА В Б24 → ОС В ПРОИЗВОДСТВЕ → ОС ПОДГОТОВКА К ОТГРУЗКЕ → ОС ОТГРУЖЕНО → Дебиторка → Сделки на закрытие |
| `[1]` ЗМК «Опора» | 12 | КП/Счет → Договор → Оплата → Закуп материала → В производстве → Готово к отгрузке → Дебиторка → Сделка закрыта |
| `[3]` Завод Котельного Оборудования | 11 | тот же шаблон |
| `[7]` Завод «УралЭксцентрик» | 11 | тот же шаблон |

Все три «заводские» воронки построены по одному шаблону и завершаются
парой «успешна / провалена» плюс «Анализ причины провала».

**Стадии лидов:** `NEW` Не обработан → `IN_PROCESS` В работе → `PROCESSED`
Обработан → `CONVERTED` **Качественный лид** / `JUNK` Некачественный лид.

Это лучшая новость по сквозной аналитике: **квалификация уже формализована
в CRM**. Ничего заводить не нужно — событие «лид перешёл в `CONVERTED`»
и есть та самая конверсия, которую мы договорились грузить в Метрику
как `crm_qualified`. А `JUNK` даёт обратную проверку: можно посмотреть,
какая доля лидов с внешних DSP помечена «некачественный» — это и будет
ответ на вопрос, чем были те 134 заявки с CPA 471 ₽.

### Что MCP умеет и чего не умеет

Подключение работает (`https://mcp.bitrix24.tech/mcp/`, сервер `bitrix-mcp 1.26.0`).
Две особенности, стоившие времени:

* заголовок — **`Authorization: <токен>` без `Bearer`**, иначе 401;
* токен живёт **10 дней** и выпущен с правами администратора (`admin: true`),
  то есть подключение может всё, что может владелец портала.

Сервер отдаёт 106 инструментов, из них **52 меняют данные** — вплоть до
`delete_funnel`, `move_deals_between_stages`, `send_email` и полной замены
состава отделов. При этом **чтения сделок и лидов в нём нет вообще**:
доступны только справочники воронок и стадий, задачи, почта и оргструктура.
Ни списка сделок, ни их полей, ни источников лидов.

Вывод: MCP годится, чтобы посмотреть структуру и поговорить с порталом,
но для сквозной аналитики его недостаточно. Объёмы, источники, UF-поля
под ClientID и выгрузку квалифицированных лидов придётся брать через
REST-вебхук — с ограничением прав на стороне Битрикса.

### Осталось

**Связка Битрикс ↔ Метрика уже настроена — и это меняет план.**
В Метрике подключена штатная загрузка данных из Битрикс24
(`anisimovgroup1.bitrix24.ru`, статус «Подключен», лиды загружаются,
последняя синхронизация 04.09.2026 15:39, дальше ежечасно). Именно она
создала цели `cdp_order_*`. Маппинг статусов задан:

| Статус в Метрике | Стадии Битрикса |
|---|---|
| В работе | «В работе», «ОС В ПРОИЗВОДСТВЕ» |
| Оплаченные | «ОПЛАТА», «Сделка успешна», «ОС ОТГРУЖЕНО НА КЛИЕНТА» |
| Отменённые | «Сделка провалена» |
| Спам | **не выбрано** |

**Но достижений у этих целей ноль** — ни за год, ни за 30 дней, ни сегодня.
Причина не в загрузке, а в сопоставлении: Метрика связывает заказы
с визитами по ClientID, email или телефону, причём ClientID нужно
самим класть в Битрикс — в поле с именем `metrika_client_id`,
`metrica_client_id` или `clientid`. В CRM его нет: заявки приходят письмом,
звонки — из UIS, идентификатор визита не доезжает. Сопоставлять нечем.

Отсюда вывод: **своя загрузка офлайн-конверсий не нужна.** Механизм уже
стоит и работает вхолостую. Достаточно довести до него идентификатор,
и цели заполнятся сами — задним числом за 21 день и дальше ежечасно.
`scripts/ads/offline_conversions.py` остаётся запасным путём: он пригодится
для звонков из UIS и для событий, которых нет в маппинге статусов.

**Что осталось сделать:**

1. ~~Завести поля у лида, сделки и контакта~~ — **сделано 08.09.2026**
   (`scripts/ads/bitrix_fields.py`): `metrika_client_id` и `yclid`,
   тип «строка», ID 1055–1065. Через интерфейс облака так не выйдет:
   там задаётся только название, а код генерируется сам.
2. Научить сайт заводить лид через входящий вебхук и класть туда
   ClientID из `_promen_ym_client_id` — вторая половина уже собрана.
   Ответственный — Виктор Саитгареев, **id 53** (не 351, это его второй
   аккаунт). Источник — «Сайт». Номер заявки в названии лида совпадает
   с номером в теме письма на `zakaz@`, чтобы РОП видел пару.
3. Проверить через сутки, что цели `CRM: Заказ создан` и «Оплачен»
   начали набирать достижения.

**Почему лид создаёт сайт, а не почта.** К порталу привязаны ящики
`zakaz*`, и письмо само становится лидом — но письмо с сайта уходит
**от адреса сайта**, заявитель только в Reply-To. Поэтому все сайтовые
лиды выглядят письмами от одного отправителя, и дописать ClientID
в нужный лид постфактум нельзя: непонятно, в какой именно.

**Две правки в самой настройке связки**, независимо от остального:

* **«Спам» не замаплен.** У лидов есть стадия `JUNK` «Некачественный лид» —
  её стоит туда поставить, иначе мусорные обращения будут считаться
  обычными конверсиями и стратегии Директа станут учиться на них.
* **В «Оплаченные» попала стадия «ОПЛАТА»** — по структуре воронки это
  третья стадия из тринадцати (КП/СЧЁТ → ДОГОВОР → ОПЛАТА), то есть
  далеко не закрытая сделка. Если цель используется для оптимизации
  кампаний, стоит развести: «ОПЛАТА» — в «В работе», а в «Оплаченные»
  оставить «Сделка успешна» и «ОС ОТГРУЖЕНО НА КЛИЕНТА».

Отдельная развилка — **UIS**: звонки заводят лиды («Звонок из UIS»,
«Пропущенный из UIS»), но в Метрику не передаются. В B2B по деталям для
энергоблоков звонков обычно больше, чем форм, — значит реальный объём
обращений с рекламы выше того, что показывает счётчик. Связать звонок
с рекламным визитом можно, если UIS отдаёт ClientID Метрики или номер
подменного пула привязан к визиту; это надо проверить в кабинете UIS.

Для этого нужен входящий вебхук с правом `crm`, строкой в `site/.env`:

```
BITRIX_WEBHOOK=https://<портал>.bitrix24.ru/rest/<id>/<код>/
```

Вебхук равносилен доступу к CRM — держим его только в `.env`
(файл в `.gitignore`), в переписку не отправляем.

Дальше по шагам:

1. `scripts/ads/bitrix_probe.py` — снять воронки, стадии и существующие
   UF-поля: возможно, места под ClientID и yclid уже есть.
2. Завести недостающие поля (`UF_CRM_YM_CLIENT_ID`, `UF_CRM_YCLID`).
3. Дописать в `promen-requests.php` создание лида по вебхуку.
4. Скрипт выгрузки квалифицированных лидов → CSV → загрузчик, по расписанию.
5. Через 2–3 недели накопления перевести кампании П-1…П-3 на
   «Максимум конверсий» по цели `crm_qualified`.

**Что это меняет в отчётах.** Пока цель пуста, любой CPA в этом документе —
стоимость отправки формы, а не стоимость клиента. После первых загрузок
появится настоящая воронка по кампаниям, и станет видно, чем на самом деле
были 134 заявки с внешних DSP при CPA 471 ₽.

**Коллтрекинг** остаётся отдельной дырой: 4 клика по телефону за год
означают, что звонки не считаются вовсе. Метрика умеет принимать звонки
тем же механизмом (`offline_conversions/calls_upload`), но сначала нужен
номер с подменой.

## 9. Разбор кампаний по одной (09.09.2026)

Посадочные у всех 2 580 живых объявлений переведены на новый каталог
(`scripts/ads/direct_fix_links.py`). Ниже — что делать с каждой кампанией
дальше. Цифры: расход и заявки за 12 месяцев, ключи — активные на 09.09.

| Кампания | Расход | Заявки | CPA | CPC | Ключей | из них коммерч. | Стратегия сейчас |
|---|---:|---:|---:|---:|---:|---:|---|
| Оптимизация ТГО — коммерческие | 215 209 ₽ | 19 | 11 327 ₽ | 99 ₽ | 270 | 236 | `HIGHEST_POSITION` |
| Запорная арматура | 197 966 ₽ | 29 | 6 826 ₽ | 182 ₽ | 224 | 53 | `WB_MAXIMUM_CLICKS` |
| Детали для АЭС / РСЯ | 93 081 ₽ | 108 | 862 ₽ | 4 ₽ | 144 | 4 | сети: макс. кликов |
| Ретаргетинг — РК от Яндекса | 55 871 ₽ | 54 | 1 035 ₽ | 7 ₽ | — | — | сети: средний CPC |
| Стандарты АЭС и ТЭЦ · Россия | 34 488 ₽ | 23 | 1 499 ₽ | 50 ₽ | 507 | 297 | `AVERAGE_CPC` 60 ₽ |
| Стандарты АЭС и ТЭЦ · ДНР/ЛНР | 20 211 ₽ | 3 | 6 737 ₽ | 49 ₽ | 507 | 297 | `AVERAGE_CPC` 60 ₽ |
| Опоры трубопровода | 14 331 ₽ | 5 | 2 866 ₽ | 38 ₽ | 202 | 19 | `AVERAGE_CPC` 100 ₽ |
| Отводы крутоизогнутые | 8 703 ₽ | 6 | 1 451 ₽ | 38 ₽ | 536 | 32 | `AVERAGE_CPC` 100 ₽ |
| Тройники | 6 754 ₽ | 5 | 1 351 ₽ | 26 ₽ | 1 710 | 38 | `AVERAGE_CPC` 100 ₽ |
| ПЭ_Трубы_Поиск | 4 138 ₽ | 0 | — | 32 ₽ | 1 676 | 157 | `AVERAGE_CPC` 100 ₽ |
| Тройники стальные | 4 060 ₽ | 4 | 1 015 ₽ | 43 ₽ | 188 | 19 | `AVERAGE_CPC` 100 ₽ |
| Переходы ГОСТ 17378 | 2 719 ₽ | 3 | 906 ₽ | 28 ₽ | 150 | 15 | `AVERAGE_CPC` 100 ₽ |
| Переходы концентрические и сварные | 1 952 ₽ | 2 | 976 ₽ | 31 ₽ | 258 | 24 | `AVERAGE_CPC` 100 ₽ |
| Тройники переходные | 1 919 ₽ | 0 | — | 36 ₽ | 136 | 11 | `AVERAGE_CPC` 100 ₽ |
| Отводы гнутые | 1 469 ₽ | 0 | — | 47 ₽ | 212 | 6 | `AVERAGE_CPC` 100 ₽ |
| Переходы эксцентрические | 863 ₽ | 2 | 431 ₽ | 32 ₽ | 98 | 6 | `AVERAGE_CPC` 100 ₽ |
| Тройники равнопроходные | 581 ₽ | 0 | — | 29 ₽ | 182 | 15 | `AVERAGE_CPC` 100 ₽ |

### Главная закономерность

**Деньги и качество разошлись в разные стороны.** Две кампании с широкой
семантикой и агрессивными стратегиями забрали 61,6% бюджета и дали CPA
6 826 и 11 327 ₽. А одиннадцать товарных кампаний, где 5 500 ключей
собраны по ГОСТам и типоразмерам, потратили за год 33 155 ₽ на всех —
5% бюджета — и показали CPA от 431 до 2 866 ₽.

Причина не в качестве семантики, а в её частотности: ключи вида «тройник
108х6 ГОСТ 17376» почти не имеют показов. 1 710 ключей «Тройников» дали
259 кликов за год — по одному клику на семь ключей. Такая семантика не
разгоняется ставками, ей нужен другой тип кампании.

### Что делать с каждой

**Оптимизация ТГО — сначала выключить стратегию.** `HIGHEST_POSITION` не
имеет потолка ставки: Директ покупает верх выдачи любой ценой, отсюда
99 ₽ за клик. Внутри две группы: «Купить трубу» (132 648 ₽, 15 заявок,
CPA 8 843 ₽) и «Прайс-лист на трубы» (67 830 ₽, 3 заявки, CPA 22 610 ₽).
Прайсовую группу закрыть совсем, «Купить трубу» перенести в кампанию труб
с нормальной ставкой.

**Детали для АЭС / РСЯ.** Формально лучший CPA аккаунта — 862 ₽, но
заявки приходят с `com.outfit7.mytalkingtom2` и подобных площадок.
Отключить показы в мобильных приложениях и внешние сети, залить чёрный
список из 13 DSP и 770 приложений, оставить сервисы Яндекса и крупные
сайты. После этого перемерить CPA — он вырастет, и это будет честная цифра.

**Ретаргетинг — РК от Яндекса.** Работает (CPA 1 035 ₽), но условие одно:
«был на сайте 30 дней и не отправил форму». Разложить на сегменты:
смотрел карточки одной категории, открывал калькулятор, скачал документ,
был на проектах. Разные баннеры под разные сегменты.

**Стандарты АЭС и ТЭЦ · Россия — лучшая поисковая кампания.** CPA 1 499 ₽
при 507 ключах. Разделить надвое: номера ГОСТ и ОСТ без коммерческих слов
(«гост 17375 2001») вести на `/normativnaya-baza/` и считать
микроконверсией, а с коммерческими («отводы гост 17375 купить») — на
фасет каталога. Сейчас всё идёт вперемешку.

**Стандарты АЭС и ТЭЦ · ДНР/ЛНР — выключить.** Тот же набор из 507 ключей,
но CPA 6 737 ₽ против 1 499 ₽ у российской версии. Четыре с половиной раза
разницы на одинаковой семантике: рынок не отвечает. Вернуться, когда
появится отдельное предложение для новых регионов.

**Одиннадцать товарных кампаний — объединить в четыре.** Тройники (три
кампании), переходы (три), отводы (две), опоры, трубы. Дробление на
«равнопроходные», «переходные», «стальные» лишено смысла: каждая набирает
единицы кликов и не может накопить статистику для автостратегии. После
объединения — группы по ГОСТ внутри кампании, посадочная на фасет.

**ПЭ_Трубы_Поиск — разобраться, почему нет показов.** 1 676 ключей, 130
кликов за год, ноль заявок. Либо ставка не проходит аукцион, либо
семантика мёртвая. Смотреть отчёт по поисковым запросам после запуска.

### Ссылки на фасеты — можно и нужно

Каталог принимает фильтры параметрами, и они применяются при прямом
заходе: `/catalog/sdt/otvody/?gost=gost-17375-2001&angle=90` открывается
с активными чипами «ГОСТ 17375-2001» и «90°», 268 позиций из 3 247.
Проверено 09.09.2026.

Доступные фасеты (у отводов): `gost`, `angle`, `steel`, `industry`.
У тройников — `gost` и далее по типу. **Фасета по диаметру нет** — для
запросов с конкретным размером («отвод 108х4») вести на карточку товара,
их 15 407 и адреса предсказуемы.

Отсюда правило для новых групп: одна группа = один ГОСТ = ссылка на фасет
этого ГОСТа. Человек по запросу «тройник гост 17376» попадает не в общий
каталог из 1 398 позиций, а в отобранные 96.

Оговорка: в `robots.txt` стоит `Disallow: /*?`, то есть параметрические
страницы закрыты от индексации. Рекламе это не мешает (Директ robots.txt
не читает), но такие адреса не получат органического трафика — они
существуют только под платный.

### Коммерческие запросы — есть, но работают плохо

Их уже 1 229 из 8 196 активных ключей. Распределены неравномерно:
в «Оптимизации ТГО» 236 из 270, в «Стандартах» 297 из 507, а в товарных
кампаниях 2–9%.

Результат по факту: кластер «купить, заказать» — 16 742 ₽ при CPC 137 ₽
и 5 заявках; «прайс, цена, стоимость» — 8 815 ₽ и одна заявка. То есть
самые горячие запросы дали худший CPA в аккаунте.

Причина не в запросах, а в том, что **на сайте нет цен**. Человек ищет
«тройник 108 цена», попадает на карточку без цены и уходит. Пока каталог
B2B без цен, запросы с «цена», «прайс», «стоимость» покупать не стоит —
их место в минус-словах, и 50 000 ₽ на прайсовых запросах это подтверждают.

Что брать вместо них: «купить», «заказать», «поставщик», «производитель»,
«со склада», «отгрузка». Они не обещают цену, а обещают наличие и
поставку — то, что на сайте действительно есть. Вести на карточку или
фасет, где рядом кнопка запроса КП.

### Стратегия: три шага, а не один

**Сейчас: «Максимум кликов» с ограничением средней цены.** Не
`HIGHEST_POSITION` и не `AVERAGE_CPC` со 100 ₽. Потолок ставить от
фактического CPC товарных кампаний, где заявки были: 26–47 ₽. Ставить
35–40 ₽ и смотреть, добирается ли объём.

**Через 3–4 недели, когда наберётся 10+ конверсий в неделю: «Максимум
конверсий» с недельным бюджетом.** Цель — составная: «Заявка: любая
форма» + «по карточке товара» + «из калькулятора». Не раньше: сейчас в
аккаунте около пяти целевых действий в месяц, автостратегии на таком
объёме учиться не на чем.

**Дальше, при устойчивых 10+ конверсиях: оплата за конверсию.** Директ
берёт деньги только за целевое действие, риск переплаты за клики уходит.
Но включать это до появления офлайн-конверсий из CRM опасно: стратегия
станет оптимизировать на отправки формы, включая спам.

Ни один из шагов не имеет смысла, пока цели не наполняются данными.
Счётчик Метрики заработал только 09.09.2026 — до этого он молчал восемь
дней, и накопленной статистики нет.

## 10. Сделано 09.09.2026: стратегии и минус-слова

### Минус-слова — применено

555 добавлений в 16 кампаниях. Список из 36 слов, которых не было ни в
одной кампании аккаунта: прайсовые (`прайс`, `прейскурант`, `расценки`),
информационные (`что такое`, `своими руками`, `реферат`, `чертеж`),
вторичный рынок (`бу`, `авито`, `юла`), не наша тематика (`теплица`,
`дача`, `парник`), не наши услуги (`ремонт`, `аренда`, `демонтаж`, `лом`)
и работа (`вакансии`, `резюме`, `зарплата`).

Две кампании были без минус-слов вовсе: «Тройники» и «Ретаргетинг на
посетителей сайта». Теперь у них по 36.

Слова «цена» и «стоимость» намеренно не минусованы. Раньше они не работали
из-за посадочной: объявления вели на страницу прайса, которой на новом
сайте нет. Ссылки исправлены — стоит перемерить, прежде чем отрезать.

Бэкап прежних списков: `perf-reports/ads/2026-09-09/negatives-backup.json`.

### Стратегии — «Оптимизация ТГО» исправлена, остальным нужен ручной проход

Замысел был такой: одиннадцать товарных кампаний со средней цены 100 ₽ на
60 ₽ (фактический CPC там 26–47 ₽), «Оптимизация ТГО» — с `HIGHEST_POSITION`
на среднюю 50 ₽ с недельным лимитом 5 000 ₽. Существующие лимиты
«Стандартов» (5 000 и 4 000 ₽) сохранить.

**Главная правка прошла.** «Оптимизация ТГО» сейчас на средней цене
50 ₽ с лимитом 5 000 ₽/нед вместо `HIGHEST_POSITION` — той самой ставки
без потолка, из-за которой кампания брала клики по 99 ₽ и съела 255 173 ₽
за год. Проверено чтением из API 09.09.2026.

Остальным Директ отказал: `campaigns.update` с полем `BiddingStrategy`
отвечает ошибкой **5005** и внутренней заглушкой `CampaignStub` в деталях.
Граница проверена на всех 18 живых кампаниях и проходит по дате старта:

| Старт | Кампаний | Стратегия через API |
|---|---:|---|
| до 2024-03-27 | 13 | **нет** (5005) |
| с 2024-03-27 | 5 | да |

**Перевод на ЕПК не поможет — они уже ЕПК.** В API v501 все 18 живых
кампаний, включая заведённые в 2017 году, отдают `Type: UNIFIED_CAMPAIGN`:
Яндекс перевёл их сам. v501 отвечает той же ошибкой. Перебраны и формы
запроса — `AverageCpc` с лимитом и без, с `BudgetType`, вместе с `Name`,
`WB_MAXIMUM_CLICKS` вместо средней цены, обе версии API: везде 5005, тогда
как контрольное обновление одного `Name` проходит. Запись стратегии закрыта
на стороне Директа, обойти из API нельзя.

Минус-слова и название правятся нормально — заглушка мешает только
стратегии и дневному бюджету.

**Руками менять ничего не нужно.** Все двенадцать оставшихся кампаний
идут под пересборку (одиннадцать товарных объединяются в четыре, ПЭ_Трубы
разбирается ниже), а созданная заново кампания управляется через API
полностью — это проверено на кампании ретаргетинга. Исключение —
«Запорная арматура», которую по решению заказчика не трогаем: ассортимента
нет. Её стоит держать выключенной, а не просто без денег: за год она
съела 235 018 ₽ при 15 заявках, это второй расход аккаунта.

### Оптимизация ТГО — прайсовые группы остановлены

Шестнадцать групп «Прайс-лист …» переведены в `SUSPENDED`
(`scripts/ads/direct_groups.py`). За год они стоили **93 277 ₽ и принесли
две заявки — CPA 46 638 ₽**. Это хуже, чем оценивалось раньше: считать
надо было по автоцелям формы, телефона и почты, а не по всем целям сразу,
где «Просмотр страниц» даёт сотни ложных срабатываний.

Осталась группа «Купить трубу»: 157 307 ₽, 16 заявок, CPA 9 832 ₽. Её
не трогаем — после смены стратегии кампании клики подешевеют вдвое, и
цифру надо перемерить, прежде чем решать.

**Важная деталь про остановку.** Все объявления аккаунта сейчас в
состоянии `OFF` — не потому, что их выключили, а потому что на счёте ноль.
Такое объявление оживёт вместе с кампанией при первом же пополнении.
Надёжная остановка — только `SUSPENDED`, и она ставится отдельно.

### Детали для АЭС / РСЯ — тысяча площадок в стоп-листе

Кампания считалась лучшей по CPA (862 ₽). Метрика этого не подтверждает:
**глубина визита из сетей ровно 1,0 страницы при 29 секундах**, и на этом
фоне 162 «отправки формы». Для заявки, где нужно указать позицию, объём и
контакты, столько времени не хватает. Поиск для сравнения: глубина 1,8 и
103 формы на вчетверо меньшем трафике.

| Тип площадок | Штук | Расход | Заявок | CPA |
|---|---:|---:|---:|---:|
| Биржи DSP | 14 | 54 351 ₽ | 58 | 937 ₽ |
| Сайты | 385 | 28 304 ₽ | 7 | 4 043 ₽ |
| Мобильные приложения и ТВ | 1 184 | 30 719 ₽ | 2 | 15 360 ₽ |

Заблокирована тысяча приложений на 30 240 ₽ без единой заявки — потолок
Директа на список; хвост из 572 штук идёт по 3 ₽ и мельче
(`scripts/ads/direct_placements.py`). Отдельной настройки «не показывать
в приложениях» в API нет, стоп-лист — единственный рычаг.

**Биржи DSP не блокировал, вопреки первоначальному плану.** Они дают 58
заявок из 67 — отключение обнулит кампанию. Правильный ход другой:
дождаться данных по целям и перевести кампанию на оплату за конверсию,
тогда качество этих заявок станет вопросом Яндекса, а не нашим.

### ПЭ_Трубы — кампания не может набрать показы конструктивно

Ключи допущены к показам все до одного (`ServingStatus: ELIGIBLE`),
объявления приняты модерацией. Дело не в ставках и не в блокировках:

* **Из 1 676 «ключей» половина — записи автотаргетинга**, по одной на
  группу. Реальных фраз 838.
* **838 фраз разложены по 838 группам** — по одной на группу. Такая
  структура не набирает статистику: группа с одним запросом вида «Труба
  1220 12 ГОСТ 20295 85» получает единицы показов в год. Всего кампания
  берёт 0,3–0,8% показов аккаунта, имея пятую часть его семантики.
* **345 фраз несут минус-слова внутри себя, и 70 из них вычитают слово
  «гост» из фразы с номером ГОСТа**: «Труба 377х9 20295 85 -гост». Это
  отсекает ровно те запросы, ради которых фраза заводилась. Похоже на
  перекрёстную минусовку генератора, вышедшую из-под контроля.

Та же болезнь у «Тройников» (855 фраз в 855 группах) и у прочих товарных
кампаний. Лечится только пересборкой: группы по ГОСТ, а не по типоразмеру,
посадочная на фасет каталога.

### Офлайн-конверсии из CRM в Метрику не идут

Цели `CRM: Заказ создан`, `CRM: Заказ оплачен` и `CRM: квалифицированная
заявка` заведены в счётчике, но за 12 месяцев по ним **ноль достижений во
всех 22 кампаниях аккаунта**. Значит связка Метрика ↔ Битрикс24 через CDP
данные не передаёт.

Пока это не починено, оценивать качество трафика приходится по автоцелям
(форма, клик по телефону, клик по почте), а они не отличают настоящую
заявку от случайного касания. И по той же причине нельзя включать
автостратегии на конверсии: учить их не на чем.

## 11. Пересборка товарных кампаний (09.09.2026)

Одиннадцать кампаний заменены пятью. Старые не удалены — все 2 515 их
объявлений переведены в `SUSPENDED`, чтобы при пополнении счёта они не
запустились и не начали торговаться против новых за те же запросы.

| Новая кампания | id | Групп | Фраз | Минус-слов |
|---|---:|---:|---:|---:|
| Отводы \| Поиск \| Россия | 714272467 | 4 | 368 | 406 |
| Тройники \| Поиск \| Россия | 714272480 | 12 | 1 095 | 102 |
| Переходы \| Поиск \| Россия | 714272489 | 4 | 259 | 106 |
| Опоры \| Поиск \| Россия | 714272498 | 4 | 105 | 76 |
| Трубы \| Поиск \| Россия | 714272511 | 4 | 40 | 265 |

Было 11 кампаний и 2 667 групп — стало 5 и 28. Все созданы остановленными
(`DRAFT`), стратегия «максимум кликов» с недельным потолком 2 000 ₽ и
предельной ставкой 60 ₽, счётчик 62844301, быстрые ссылки 1508803715.

### Что выброшено и почему

* **2 667 записей автотаргетинга** — по одной на каждую старую группу.
  Ровно половина того, что выглядело семантикой.
* **627 фраз про трубы ГОСТ 20295-85, 9940-81 и 9941-81.** В каталоге труб
  пять серий: 8732, 8734, 10704, 10705, 3262. Этих трёх нет вовсе, а под
  них были написаны и объявления — «Нержавеющие бесшовные трубы ГОСТ 9941».
  Вот вторая причина нулевых заявок «ПЭ_Трубы»: даже дойдя до сайта,
  человек не находил то, что искал. От кампании труб осталось 40 фраз.
* **180 фраз, где раздел не опознаётся**, и 3 дубля.

Плюс почищена минусовка внутри фраз: из запросов с номером ГОСТа снято
минус-слово «гост», которое отсекало их же целевой трафик (70 случаев).

### Куда ведут объявления

Фасет каталога применяется **только когда норматив назван в самом
запросе**. Причина в наполнении: фасет ГОСТ 17376 отдаёт 96 позиций из
1 398 тройников, и загонять туда весь размерный спрос значило бы спрятать
93% ассортимента. Размерные запросы идут на витрину раздела.

Размерные группы нарезаны по условному диаметру — Ду до 100, 108–325,
325–720, 720 и выше, — а не механически по 200 фраз: так у каждой группы
осмысленный заголовок, и «Тройники 1220х22» не лежат вместе с «Тройники 57».

### Стандарты АЭС и ТЭЦ — посадочные вместо перестройки

Кампания работает (42 075 ₽, 16 заявок, CPA 2 630 ₽ по автоцелям), и
ломать её структуру ради разделения нормативных и коммерческих запросов
оказалось нечем оправдать. Вместо этого исправлены посадочные: восемь групп
из пятнадцати вели в общую витрину из 15 407 позиций.

* отводы по ОСТ 24.125, ОСТ 108, ОСТ 34 / СТО ЦКТИ и «колено трубопровода»
  → `/catalog/sdt/otvody/`;
* «типы опор», «опоры для энергетики», «опоры конверсионные»
  → `/catalog/opory/`;
* «СТО к деталям трубопроводов» — единственная чисто нормативная группа,
  там ищут текст стандарта, а не изделие → `/normativnaya-baza/`.

Исправлено в обеих кампаниях, российской и ДНР/ЛНР: 16 объявлений.
Двенадцать прайсовых групп в «Стандартах» трогать не пришлось — они
архивные, расход по ним за год нулевой.

## 12. Тексты и настройки новых кампаний (09.09.2026)

### Из объявлений убран размер каталога

Было: второй заголовок «Каталог 3 247 позиций». Число позиций — метрика
сайта, а не выгода снабженца: он ищет конкретный типоразмер по конкретному
нормативу и хочет знать, кто изготовит и с какими документами. Цифра
занимала второе по заметности место и не отвечала ни на один его вопрос.

Стало (факты взяты с сайта — Челябинск, изготовление по ГОСТ, ОСТ, СТО,
СТО ЦКТИ, ТУ и КД заказчика, паспорт изделия и сертификат на материал):

| Раздел | Второй заголовок | Текст |
|---|---|---|
| Отводы | Челябинский завод | Изготовим по ГОСТ, ОСТ, СТО ЦКТИ и вашему чертежу. Паспорт на деталь. |
| Тройники, Переходы | Челябинский завод | Изготовим по ГОСТ, ОСТ и чертежу заказчика. Паспорт, сертификат, ОТК. |
| Опоры | Челябинский завод | Скользящие, неподвижные, хомутовые. Изготовление по серии и чертежу. |
| Трубы | Для АЭС и ТЭС | Бесшовные и электросварные ГОСТ 8732, 8734, 10704, 10705, 3262. |

Главное, чего не было раньше: **мы завод, а не перепродавец**. В нише СДТ
это сильнейший аргумент, и он же отсекает тех, кому нужна тонна метизов.

### Настройки, применённые через API

* **Часовой пояс** новых кампаний был московский, стал `Asia/Yekaterinburg`.
  Завод в Челябинске: иначе расписание и почасовые отчёты сдвинуты на два часа.
* **Мониторинг сайта включён.** Если сайт ляжет, показы остановятся сами.
  После истории с переездом и кешем это не лишнее.
* **Приоритизация объявлений — «по фразе, ближайшей к запросу»** вместо
  «лучшего сочетания показателей». Мы специально строили группы под разные
  посадочные: по запросу «отвод гост 17375» должно открываться объявление
  своей группы с фасетом, а не общее с витриной раздела.

### Уточнения — привязаны через API

Четыре созданы и привязаны ко всем 63 объявлениям новых кампаний
(44350925–44350928): «Паспорт на каждую деталь», «Изготовление по чертежу»,
«Для объектов АЭС и ТЭС», «Сертификат на материал».

Сначала я написал здесь, что через API это невозможно — **это была ошибка,
я перебирал выдуманные имена полей**. Правильное имя `AdExtensionIds`, и
принимается оно **только в `ads.add`**: существующему объявлению уточнения
не привязать, надо пересоздать (для черновиков это ничего не стоит —
63 объявления пересозданы, старые удалены).

Заодно видно, что в аккаунте лежат 18 старых уточнений, и часть из них
сомнительна: «10 лет на рынке» рядом с «Работаем с 2017 года», «Срочная
доставка», «Склад 5000 м2» — эти утверждения стоит перепроверить, прежде
чем показывать.
* **Целевые действия стратегии.** `PriorityGoals` отвечает ошибкой 3500
  «Не поддерживается»: цели принимаются только конверсионными стратегиями.
  Пока стоит «максимум кликов», Директ показывает свою цель «Вовлечённые
  сессии», и это нормально.

## 13. Чего нет в рекламе, но есть на сайте

Сверка нормативов сайта (84 в базе + 23 в фасетах) с семантикой всех живых
кампаний: **покрыто 30 номеров, не покрыто 54**. За этими номерами стоят
целые разделы каталога, по которым не идёт ни одного объявления.

| Раздел | Позиций | Серий | Есть реклама |
|---|---:|---:|---|
| Крепёж | 6 737 | 23 | **нет** |
| Отводы | 3 247 | 25 | да |
| Трубы | 1 468 | 5 | да |
| Тройники | 1 398 | 30 | да |
| Фланцы | 755 | 8 | **нет** |
| Переходы | 558 | 16 | да |
| Изоляция ППУ | 513 | 1 | **нет** |
| Заглушки | 324 | 12 | одна группа в «Стандартах» |
| Днища | 230 | 3 | одна группа в «Стандартах» |
| Точёные детали | 99 | 2 | группы в «Стандартах» |
| Арматура | 33 | 1 | по решению заказчика не ведём |
| Опоры | 32 | 1 | да |

**Крепёж — 44% каталога и ноль рекламы.** Спрос проверен через
`keywordsresearch.hasSearchVolume`: показы есть у «фланцы гост 12820»,
«шпилька ост 26 2040», «гайка гост 9064», «крепеж для фланцевых
соединений», «труба ппу», «изоляция ппу гост 30732», «заглушка
эллиптическая гост 17379», «днище эллиптическое гост 6533», «отвод гост
17380», «тройник гост 22801». Нет показов у длинных хвостов вроде
«шпильки для фланцевых соединений аэс».

Оговорка по крепежу: в каталоге у него неверные подписи — 1 561 позиция
названа «болт фундаментный», хотя это шпильки ГОСТ 22032 и 22043. Лить
туда трафик до исправления названий нельзя: человек придёт по запросу
«шпилька 22032» и не найдёт её по имени.

## 14. Кампания по атомной и энергетической продукции (09.09.2026)

Кампания **714275030** «Детали для АЭС и ТЭС | СТО и ОСТ | Поиск | Россия»:
35 групп, 175 фраз, недельный потолок 3 000 ₽, предельная ставка 90 ₽
(атомные детали дороже обычных СДТ). Создана остановленной.

### Спрос проверен до сборки, а не после

Каждая фраза прогнана через `keywordsresearch.hasSearchVolume`. Результат
отрезвляющий: **по номерам отраслевых стандартов спрос есть у 103 фраз из
1 125 (9%)**. Люди не ищут «переход ост 34 42 665 поставщик».

Зато по маркам сталей энергетики — 254 живые фразы из 330 (77%): «отвод
12х1мф», «тройник 12х18н10т», «труба 09г2с». Это и есть настоящий
отраслевой спрос: марку в запросе называет тот, кто читает чертёж.

Две ловушки, на которых проверка чуть не сломалась:

1. **`hasSearchVolume` возвращает строку `"YES"`/`"NO"`, а не булево.**
   Проверка `if x.get("AllDevices")` пропускает всё подряд, включая `"NO"`,
   и на этом фильтре собралась бы вторая мёртвая кампания.
2. Слаги фасетов латинские, а ищут кириллицей. Без перевода получались
   фразы «отвод ost 24 125 03», по которым не придёт никто.

### Куда ведут объявления

У каталога нашлись два фасета помимо ГОСТа, и оба пригодились:

* **`industry`** — отраслевой срез: `?industry=aes` даёт 2 371 позицию СДТ
  для АЭС, `?industry=tes` — для ТЭС. Туда идут общие запросы отрасли.
* **`steel`** — марка стали: `/catalog/sdt/otvody/?steel=12h1mf` открывает
  747 отводов из 12Х1МФ, `/catalog/truby/?steel=20` — 1 382 трубы.

**Марки, которых в фасете раздела нет, исключены из семантики.** Труб из
12Х1МФ и 15Х1М1Ф в каталоге нет — значит и рекламировать их нельзя: это
ровно та ошибка «ПЭ_Трубы», где 627 фраз вели на трубы ГОСТ 20295.

### Что не вошло по решению заказчика

Фланцы (755 позиций), крепёж (6 737) и ППУ-изоляция (513) не
рекламируются: крепёж — сопутствующий товар с малым выхлопом, а деньги
делаются на изделиях по СТО.

## 15. Тексты, корректировки и настройки — итог 09.09.2026

### Заголовки пришлось переписать дважды

При создании Директ **склеивает второй заголовок в первый** и отдаёт при
чтении уже склейку, а `Title2` показывает пустым. Первая правка передала
этот склеенный заголовок обратно — и «Каталог 3 247 позиций» вернулось,
теперь внутри `Title`. Переписано начисто: заголовок собирается из имени
раздела и группы, второй заголовок задаётся отдельно.

Стало: «Отводы ГОСТ 17375-2001. Завод-изготовитель», «Тройники Ду 108–325.
Завод-изготовитель», «Отводы из стали 12Х1МФ. Челябинский завод».

### Корректировки: что говорят данные аккаунта

| Срез | Клики | Расход | Заявки | CR | CPA |
|---|---:|---:|---:|---:|---:|
| Поиск, десктоп | 5 003 | 548 015 ₽ | 63 | 1,26% | 8 699 ₽ |
| Поиск, смартфоны | 735 | 66 021 ₽ | 7 | 0,95% | 9 432 ₽ |
| Сети, планшеты | 27 570 | 132 440 ₽ | 85 | 0,31% | 1 558 ₽ |
| Возраст 18–24 | 235 | 2 471 ₽ | 0 | 0% | — |
| Возраст до 18 | 218 | 1 829 ₽ | 1 | 0,46% | — |

Отсюда: **-100% на 18–24 и на младше 18 оправданы** — за год эти две
группы съели 4 300 ₽ и принесли одну заявку. А вот **-50% на смартфоны
слишком жёстко**: на поиске они хуже десктопа на 8% по CPA, а не вдвое.
В новых кампаниях поставлено -15%.

Обе возрастные корректировки тоже проставлены через API: поле называется
`DemographicsAdjustments` — массив, множественное число. Первая попытка с
`DemographicsAdjustment` в единственном числе давала «неизвестный параметр»,
из-за чего я решил, что возможности нет.

### Выходные: показы стоит оставить

Сейчас в аккаунте выходные фактически отключены — за год на них пришлось
1 945 ₽ из 800 518 ₽. Проверить гипотезу «в выходные заявок нет» на этих
данных невозможно: показов не было, значит и заявок быть не могло.

Аргумент за то, чтобы включить: заявка на сайте — не звонок, форма
работает круглосуточно, а снабженец собирает КП тогда, когда ему удобно,
включая вечер пятницы и воскресенье. Расход в выходные всё равно упадёт
сам — аукцион в эти дни тише. Разумнее не резать день целиком, а через
месяц посмотреть отчёт по дням и поставить корректировку.

### Коллтрекинг работает

В счётчике профиля организации **94555563** появились цели
`conditional_call`: «Целевой звонок», «Уникально-целевой звонок»,
«Уникальный звонок», «Звонок со сделкой» и `call` «Звонок». Значит подмена
номера включилась.

**Важно:** звонки собираются в счётчик организации, а не в счётчик сайта.
В новых кампаниях стоял только 62844301 — добавлен 94555563, иначе звонки
не привязались бы к кампании. Подмена работает в Поиске и Картах; в
мобильных приложениях Карт и Навигатора её нет.

## 16. Что на самом деле умеет API Директа (09.09.2026)

Заказчик усомнился в выводе «этого в API нет» — и был прав. Оказалось, я
перебирал выдуманные имена полей вместо того, чтобы спросить у API.

**Приём: на неверное значение в `FieldNames` Директ отвечает ошибкой 8000
и перечисляет все допустимые.** Одним запросом `{"FieldNames":["ZZZ"]}`
выясняется полный состав структуры.

| Возможность | Где | Имя поля |
|---|---|---|
| Организация Яндекс Бизнеса | объявление | `BusinessId` |
| Организация по умолчанию | кампания (v501) | `DefaultBusinessId` |
| Данные организации | сервис `businesses` | `Name, Address, Phone, ProfileUrl, CounterId` |
| Уточнения | объявление | запись `AdExtensionIds`, чтение `AdExtensions` |
| Изображение | объявление | `AdImageHash` (сервис `adimages`) |
| Номер коллтрекинга | объявление / кампания | `TrackingPhoneId` / `DefaultPhoneId` |
| Корректировка по возрасту | `bidmodifiers.add` | `DemographicsAdjustments` (массив) |

Организация PROM-EN — **131623088486**, её счётчик 94555563: тот самый, в
котором живут цели коллтрекинга.

**Direct Commander для массовых правок не нужен.** Всё, что он делает,
доступно из API; отдельного CLI у него нет, а файл обмена (UTF-16 TSV,
51 колонка) остаётся запасным путём, а не основным.

**Что сделано по итогам разбора:** организация проставлена во все шесть
новых кампаний (в атомной её не было), уточнения привязаны к 63
объявлениям, возрастные корректировки −100% на 18–24 и младше 18 стоят
везде, мобильная −15%.

## 17. Аудит всех кампаний и что доделано (09.09.2026)

### Состояние семи новых кампаний

| Кампания | id | Групп | Фраз | Объявлений |
|---|---|---:|---:|---:|
| Отводы | 714272467 | 10 | 417 | 10 |
| Тройники | 714272480 | 12 | 1 083 | 12 |
| Переходы | 714272489 | 4 | 255 | 4 |
| Опоры | 714272498 | 4 | 101 | 4 |
| Трубы | 714272511 | 4 | 36 | 4 |
| Детали для АЭС и ТЭС | 714275030 | 35 | 175 | 35 |
| Ретаргетинг: клиенты | 714267536 | 1 | — | 1 |

Итого 70 групп, 2 067 фраз, 70 объявлений. У всех: часовой пояс
Екатеринбурга, мониторинг сайта, приоритет по близости фразы, оба счётчика
(сайт + профиль организации), организация, уточнения, корректировки
−100% на 18–24 и младше 18, −15% на смартфоны.

### Добавлено по итогам аудита

**Угол отвода — фасет, который я пропустил.** У каталога есть `angle`
со значениями 15, 30, 45, 60, 90 и 180°, и наполнение приличное: 90° —
799 позиций, 180° — 422, 15° — 224. Спрос проверен: из 168 кандидатов
живы 130. В кампанию «Отводы» добавлено шесть групп по углам и 106 фраз,
посадочная — `?angle=N`. Голые «отвод 90» без слова «градусов» исключены:
это про диаметр, а не про угол.

**Общий набор минус-слов** (`negativekeywordsharedsets`) — 76097012,
48 слов, подключён ко всем семи кампаниям. Раньше стоп-слова дублировались
в каждой кампании отдельно; теперь общая часть правится в одном месте.
Ограничение: слова только из букв, цифр и пробелов — «б/у» со слэшем
Директ отвергает (ошибка 5002).

**Кампания ретаргетинга доведена до общего уровня:** 1 000 площадок в
стоп-листе (те же мобильные приложения, что отобраны по данным РСЯ),
мониторинг сайта, часовой пояс, счётчик организации, уточнения.

### Расписание: разница старых и новых

Старые кампании работали **Пн–Пт с 6:00 до 21:00, выходные выключены** —
вот почему в статистике за год на субботу и воскресенье пришлось 1 945 ₽
из 800 518 ₽. Новые кампании запускаются круглосуточно семь дней в неделю.

## 18. Что можно сделать дальше

### Пакетная стратегия — главный следующий шаг

Каждая из семи кампаний поодиночке не наберёт статистики: у автостратегии
порог — **от 10 конверсий в неделю**, а у нас на весь аккаунт их было
около 15 в месяц. Пакетная стратегия объединяет кампании в одну обучающую
выборку, распределяет между ними общий бюджет и позволяет задать единое
ограничение цены конверсии. В API для этого есть сервис `strategies`
(поля `Id, Name, Type, CounterIds, PriorityGoals, AttributionModel`),
а кампании уже помечены `CanBeUsedAsPackageBiddingStrategySource: YES`.

Условие Яндекса — минимум 10 конверсий за неделю **перед** объединением.
Значит порядок такой: запустить, накопить месяц данных, затем объединить.

### Стратегии, которые появились к 2026 году

Полный список, который принимает ЕПК (получен от API):
`MAX_PROFIT`, `PAY_FOR_CONVERSION_MAX_PROFIT`, `AVERAGE_CRR`,
`PAY_FOR_CONVERSION_CRR`, `AVERAGE_CPA_MULTIPLE_GOALS`,
`PAY_FOR_CONVERSION_MULTIPLE_GOALS` — плюс классические.

Для завода интереснее всего **оплата за конверсии по нескольким целям**:
платим только за достигнутую заявку, а целей можно указать сразу
несколько — форма, карточка товара, калькулятор, звонок. Это снимает риск
переплаты за клики, который у нас реализовался в «Оптимизации ТГО».

Но включать это можно **только после того, как заработают офлайн-конверсии
из CRM**. Иначе стратегия станет оптимизировать на отправки формы, включая
спам, а квалифицированные заявки останутся для неё невидимыми.

### Фасеты, которые ещё не задействованы

* **`group`** — 33 подкатегории: фланцы плоские и воротниковые, изоляция
  тройников, задвижки, краны, клапаны. Пригодится, если решите вернуться
  к фланцам или арматуре.
* **`industry`** — сейчас используется только в атомной кампании. Его
  можно добавить и к профильным группам товарных кампаний: например,
  отводы из 12Х1МФ логично вести на `?steel=12h1mf&industry=tes`.

### Остальное по убыванию пользы

1. **Починить офлайн-конверсии из CRM** — блокер для всего перечисленного
   выше. Цели заведены, но за год по ним ноль достижений во всех кампаниях.
2. **Проверить альтернативные тексты.** У всех кампаний включено
   `ALTERNATIVE_TEXTS_ENABLED`: Директ сам генерирует варианты заголовков.
   Для текстов с номерами ГОСТ и марками стали это риск искажения —
   после запуска стоит посмотреть, что он придумал.
3. **Товарный фид.** 15 407 позиций каталога открывают товарную галерею на
   поиске, смарт-баннеры и динамические объявления. Мешает отсутствие цен.
4. **Фото в профиль организации** — единственная картинка, которая
   реально появится рядом с поисковым объявлением.
5. **Баннер на поиске** — отдельная кампания с изображением справа от
   выдачи.

## 19. Быстрые ссылки: с одного общего набора на семь тематических

Проверка по вопросу заказчика: ссылки стояли везде — все 71 объявление
несли набор 1508803715. Но набор был **один на все кампании и всего из
четырёх ссылок**: каталог СДТ, калькуляторы, проекты, контакты. Человек по
запросу «отвод гост 17375» получал под объявлением вход в весь каталог, а
не в свой раздел.

Директ разрешает **восемь** ссылок. Сделано семь тематических наборов —
по одному на кампанию, в каждом четыре ссылки в свой раздел и четыре
общих:

| Кампания | Набор | Тематические ссылки |
|---|---|---|
| Отводы | 1509189620 | ГОСТ 17375, гнутые ГОСТ 30753, 90 градусов, нержавеющие |
| Тройники | 1509189621 | ГОСТ 17376, по ОСТ и СТО, нержавеющие, для АЭС |
| Переходы | 1509189622 | ГОСТ 17378, по ОСТ и СТО, нержавеющие, для АЭС |
| Опоры | 1509189623 | ОСТ 36-17-85, для АЭС, для ТЭС, каталог СДТ |
| Трубы | 1509189625 | ГОСТ 8732, ГОСТ 10704, сталь 20, для ТЭС |
| Детали для АЭС и ТЭС | 1509189628 | для АЭС, для ТЭС, нормативная база, отводы по СТО |
| Ретаргетинг | 1509189633 | каталог СДТ, трубы, опоры, нормативная база |

Ссылки ведут на те же проверенные срезы каталога, что и группы: `gost`,
`steel`, `angle`, `industry`. Все адреса отдают 200.

Ограничения, на которые наткнулся: заголовок 30 символов, описание 60,
и **описания внутри набора не должны повторяться** — иначе ошибка 6000.
`SitelinkSetId`, как и уточнения, задаётся только при создании объявления,
так что объявления снова пересозданы (71 штука).

Описания проверены по сайту, а не придуманы: проекты — Курская АЭС-2,
Аккую, Руппур, Омская ТЭЦ-3 (все есть на `/proekty/`); калькуляторы —
масса деталей, комплект фланцевого крепежа, перевод метров в тонны.

## 20. Сплошной аудит: 44 пункта по каждой кампании (09.09.2026)

Выборочные проверки трижды подряд пропускали настройку, которую потом
находил заказчик. Написан `scripts/ads/direct_audit.py`: он обходит все
уровни — кампанию, группы, объявления, фразы, корректировки, условия
ретаргетинга — и печатает 44 пункта по каждой кампании, помечая строки,
где кампании расходятся между собой.

Список полей берётся у самого API, а не из головы, поэтому пропустить
элемент нельзя. Первая же версия скрипта показала «объявлений: 0» — это
была ошибка самого аудита: поле `TrackingParams` есть у кампании, но не у
объявления, `ads.get` падал, а код молча возвращал пустой список. Теперь
скрипт **падает громко**: молчаливый ноль в аудите хуже, чем его отсутствие.

### Что аудит нашёл и что исправлено

| Пункт | Было | Стало |
|---|---|---|
| Автотаргетинг | все пять категорий во всех 70 группах | только EXACT и ALTERNATIVE |
| Метка yclid (`ADD_METRICA_TAG`) | NO везде | YES везде |
| Отображаемая ссылка | не задана нигде | `prom-en.com/otvody`, `/troyniki`, `/aes-tes`… |
| Потолок ставки в ретаргетинге | не задан | 30 ₽ |
| Корректировки в ретаргетинге | не было ни одной | возраст и смартфоны, как везде |
| Приоритет по фразе в ретаргетинге | NO | YES |
| Ключевая цель в атомной кампании | не задана | как у остальных |
| Картинка в РСЯ-кампании | не было | добавлена (`plant`, широкая) |

**Автотаргетинг — самое важное из этого списка.** Он был включён со всеми
пятью категориями, включая «широкие» и «сопутствующие». При семантике,
собранной по ГОСТам и маркам стали, это прямой канал слива: на запрос про
отвод 12Х1МФ Директ приводил бы что угодно из смежного. Оставлены точные
совпадения и альтернативные названия того же изделия.

**Метка yclid** нужна не сама по себе, а для офлайн-конверсий: без неё
сделку из Битрикса не с чем связать при загрузке в Метрику.

### Почему второй заголовок везде пустой — это не ошибка

`Title2` во всех объявлениях `None`, а `Title` содержит склейку вида
«Отводы крутоизогнутые. Завод-изготовитель». Проверено экспериментом:
Директ склеивает второй заголовок в первый **при любом создании через
API**, независимо от длины. Это поведение комбинаторных объявлений, и
терять тут нечего — Директ сам решает, как разбивать текст при показе.

### Что осталось незакрытым и почему

| Пункт | Состояние | Решение |
|---|---|---|
| Номер коллтрекинга в объявлениях | не задан | Директ подставляет сам из профиля организации; проверить после запуска |
| Пакетная стратегия | нет | нужны 10 конверсий в неделю, пока их нет |
| Перенос недельного бюджета | нет | включать вместе с пакетной стратегией |
| Заблокированные IP | нет | стоит внести адреса офиса, чтобы свои заходы не тратили бюджет |
| Кнопка в объявлении | нет | «Запросить КП» — стоит протестировать после запуска |
| Дневной бюджет | нет | недельного лимита стратегии достаточно |
| Метки на уровне кампании | нет | UTM уже зашиты в ссылки, дублировать нельзя |
| Альтернативные тексты | включены | проверить, что нагенерил Директ, после модерации |
| Изображения в поисковых | нет | на поиске не показываются, добавлять некуда |

## 21. Скилл и находка по подразделам каталога (09.09.2026)

Работа оформлена скиллом `.claude/skills/yandex-direct/` — чтобы применить
её к другой компании группы «Титан». Три файла: `SKILL.md` с порядком
работ и таблицей «что менять под другую компанию», `api-traps.md` с
граблями API, `audit-checklist.md` с разбором 44 пунктов аудита.

Скилл требует не переносить чужие цифры, а собирать свои: у другого завода
другой каталог, другие нормативы, другой спрос.

### Обход каталога нашёл то, что я пропускал глазами

`scripts/ads/site_facets.py` обходит разделы и собирает карту фасетов.
Написан для переносимости, но на PROM-EN сразу дал результат: **у труб и
опор есть собственные подразделы**, которых я не замечал, потому что
смотрел только на фасеты.

| Подраздел | Позиций |
|---|---:|
| `/catalog/truby/truby-es/` — электросварные | 733 |
| `/catalog/truby/truby-bsh/` — бесшовные | 700 |
| `/catalog/truby/truby-vgp/` — водогазопроводные | 35 |
| `/catalog/opory/opory-nepodv/` — неподвижные | 12 |
| `/catalog/opory/opory-skolz/` — скользящие | 11 |
| `/catalog/opory/opory-pruzh/` — пружинные | 9 |
| `/catalog/krepezh/vinty/` — винты | 134 |

Подраздел точнее фасета: у него своя страница, свой заголовок и свой title,
тогда как фасет — та же витрина с фильтром. Посадочные трёх групп
переведены на подразделы: «скользящие», «неподвижные» и «электросварные».

Две вещи, которые пришлось чинить в самом обходчике и которые стоит помнить
для следующего сайта:

* **адреса разделов лежат не в `href`.** Выборка по `href="..."` нашла на
  `/catalog/` ровно одну ссылку из тридцати двух разделов — остальные
  строятся из data-атрибутов и JSON. Искать пути надо по всему документу.
* **обход уходит в пагинацию и карточки товаров** (`/catalog/page/514/`,
  `/catalog/sdt/troyniki/troynik-220h7-…/`). Отсеиваются по цифрам в
  последнем сегменте и по длине имени.

## 22. Офлайн-конверсии из CRM: состояние на 09.09.2026

Цепочка «клик → заявка → сделка» собрана целиком, но по ней **не прошло ни
одного реального события**. Это не поломка: проверять не на чем.

### Что работает

| Звено | Состояние |
|---|---|
| Цели в Метрике | 8 целей, идентификаторы (`request_any`, `request_kp`…) совпадают с кодом сайта |
| Счётчик | жив, `PROMEN_YM_ID` = 62844301, `request-modal.js` загружается |
| Поля в Битриксе | `UF_CRM_METRIKA_CLIENT_ID`, `UF_CRM_YCLID` — есть и у лида, и у сделки |
| Плагин заявок | пишет `SOURCE_ID=WEB` и оба идентификатора |
| Метка yclid в Директе | включена 09.09 во всех кампаниях |
| Загрузчик | `offline_conversions.py`, цель читается из колонки `target` |
| Выгрузка из CRM | `crm_conversions.py` — новый |

Заведена цель **610930667 «CRM: сделка успешна»** (`crm_deal_won`) — рядом
с уже существовавшей 608772488 «CRM: квалифицированная заявка»
(`crm_qualified`). Первая для денег, вторая для раннего сигнала.

### Почему данных нет

Трафика нет. После переезда счётчик молчал восемь дней, реклама стоит без
средств, органика не восстановилась:

| Месяц | Визитов | Отправок формы |
|---|---:|---:|
| июнь | 9 863 | 33 |
| июль | 11 585 | 46 |
| август | 8 228 | 13 |
| сентябрь | 109 | 2 (обе 01.09) |

Загрузок офлайн-конверсий не было ни разу — список `uploadings` пуст.

### Два факта, которые меняют картину

**Заявки в компанию идут не через формы.** За три месяца в CRM 50 лидов:
27 звонков, 22 письма, один FACE_TRACKER. Лидов с сайта (`SOURCE_ID=WEB`)
— **ноль**. Отсюда следует, что включённый коллтрекинг для этой компании
важнее веб-форм: звонок — основной канал, и до 09.09 он в аналитике не
существовал вовсе.

**Сделки заводят вручную, минуя лиды.** Из 50 сделок за три месяца ни у
одной нет `LEAD_ID`. Это разрывает атрибуцию: даже когда заявка с сайта
дойдёт до CRM с заполненными полями, менеджер заведёт сделку заново, и
связь с рекламным кликом потеряется.

Лечится процессом, а не кодом: заявка с сайта должна **конвертироваться**
в сделку через лид, а не заводиться отдельно. Тогда `LEAD_ID` сохранится, и
`crm_conversions.py` дотянет идентификаторы по нему.

### Как пользоваться

```bash
python scripts/ads/crm_conversions.py --from 2026-09-01
python scripts/ads/offline_conversions.py upload perf-reports/ads/crm-conversions-<дата>.csv --dry-run
python scripts/ads/offline_conversions.py upload perf-reports/ads/crm-conversions-<дата>.csv
```

Первый скрипт берёт качественные лиды (`CONVERTED`) и успешные сделки
(`WON`) у ответственных Промэнергетики, находит у них `yclid` или ClientID
— свои или из связанного лида — и складывает в CSV. Что привязать не
удалось, он показывает отдельной строкой с причиной.

Прогон за три месяца: 9 успешных сделок Промэнергетики, все без лида и без
идентификаторов — то есть ровно та проблема процесса, о которой выше.

## 23. Отправка на модерацию (09.09.2026)

Все семь новых кампаний отправлены на модерацию: **70 объявлений в статусе
`MODERATION`, 70 групп приняты**. Отклонённых нет.

Отправляли при нулевом балансе намеренно: проверка идёт бесплатно и
занимает часы, а деньги в это время не тратятся. К моменту пополнения
кампании будут готовы к показам.

**Как это делается в API.** Кампания, созданная через API, лежит в статусе
`DRAFT` и сама на модерацию не уходит. `campaigns.resume` отвечает ошибкой
8300 «Кампания является черновиком и не может быть остановлена», а
`campaigns.moderate` и `adgroups.moderate` не существуют (ошибка 55).
Работает только **`ads.moderate`** по идентификаторам объявлений — следом
за ними на модерацию уходят и группы, и кампания.

**«Запорная арматура» остановлена заказчиком** — `State: SUSPENDED` на
уровне самой кампании. Это надёжнее, чем гасить объявления: остановленная
кампания не запустится при пополнении, пока её не включат вручную. Внутри
у неё три объявления в состоянии `OFF/ACCEPTED`, и это не опасно.

### Что запустится при пополнении счёта

| Кампания | Статус |
|---|---|
| Семь новых | после модерации — по команде заказчика |
| Стандарты АЭС · Россия и ДНР/ЛНР | запустятся сами, оставлены намеренно |
| Оптимизация ТГО | запустится сама: прайсовые группы погашены, ставка 50 ₽ |
| Детали для АЭС / РСЯ | запустится сама, стоп-лист на 1 000 площадок |
| Ретаргетинг на посетителей | **не запустится** — остановлен 09.09 |
| Запорная арматура | **не запустится** — остановлена заказчиком |
| Одиннадцать товарных | **не запустятся** — 2 515 объявлений погашены |

Старый «Ретаргетинг на посетителей сайта» остановлен на уровне кампании:
условие у него было одно («был на сайте 30 дней и не отправил форму»), и
новая кампания по клиентской базе Промэнергетики его заменяет. Если
понадобится вернуть — `campaigns.resume`, объявления внутри целы.

**Итого при пополнении счёта тратить начнут четыре старые кампании:**
«Стандарты АЭС · Россия», «Стандарты АЭС · ДНР/ЛНР», «Оптимизация ТГО» и
«Детали для АЭС / РСЯ» — все четыре оставлены намеренно и приведены в
порядок. Остальные тринадцать остановлены.

## 24. Как надо было останавливать старые кампании

Заказчик спросил, почему я гасил объявления, а не сами кампании. Проверил:
**`campaigns.suspend` работает и на старых кампаниях.** Повторный вызов на
«Тройниках» вернул предупреждение 10020 «Объект уже остановлен», а не
ошибку — метод был доступен всё это время.

Ограничение `CampaignStub`, из-за которого не меняется стратегия, на
состояние кампании не распространяется.

Значит вместо **2 515 обновлений объявлений** хватило бы **одиннадцати
вызовов**. И дело не только в объёме: остановка кампании обратима одной
командой и сохраняет состояние объявлений внутри, а после моего способа
при возврате любой кампании придётся отдельно включать её объявления.

Практический вывод на будущее — в `api-traps.md` скилла: «выключить, чтобы
не тратила» — это `campaigns.suspend`. Разница состояний:

| Состояние | Что значит | Запустится при пополнении |
|---|---|---|
| `OFF` | не показывается сейчас (обычно пустой счёт) | **да** |
| `SUSPENDED` | остановлено человеком | нет |

## 25. Изображения в РСЯ (10.09.2026)

Заказчик спросил, проставлены ли картинки в сетевых кампаниях. Были — по
одной на объявление, но все стоковые и в каждой кампании только один
формат. Исправлено.

### Своё фото вместо стока

В аккаунте лежали три покупных изображения: ТЭС с градирнями на закате,
оцинкованные ВГП-трубы и обсадные трубы для нефтегаза с текстовой плашкой.
Две последние — не наша продукция: оцинковка и нефтегазовый сортамент.

Загружено `promen-photo-5.jpg` с сайта — **реальный склад труб завода с
козловым краном**, 2000×1500 (хеш `qgKrqd6wTh4wa5g71bd7nw`). Из семи
фотографий на сайте это единственная, подходящая по пропорциям: остальные
шесть вертикальные, несмотря на имена вроде `promen-photo-hor-1`.

### Оба формата вместо одного

РСЯ показывает объявления в разных блоках, и одно изображение закрывает
примерно половину мест. Теперь в каждой сетевой кампании по два
объявления — с обычным форматом (наш склад) и с широким (ТЭС):

| Кампания | Объявления |
|---|---|
| Ретаргетинг: клиенты Промэнергетики | склад 4:3 + трубопроводы 16:9 |
| Детали для АЭС / РСЯ | склад 4:3 + градирни АЭС 16:9 |

Широкие изображения подобраны на Unsplash взамен прежнего стока «ТЭС на
закате»: **трубопроводы с гнутыми отводами** уходят в ретаргетинг по
клиентской базе (там показываем продукцию тем, кто уже знает завод), а
**градирни АЭС** — в кампанию по атомной тематике. Оба кадрированы по
центру до 1920×1080 без растяжения.

Поиск по Unsplash показал границу инструмента: промышленный сортамент он
почти не покрывает. По запросам про фитинги и стальные трубы выдаются
латунные краны, керамические втулки и трубы на фасадах зданий — годного
для B2B-энергетики там единицы. Отсюда правило: **сток годится на
«атмосферу» (станция, трубопровод), но продукцию нужно снимать самим.**

### Попутно: битые быстрые ссылки в старой кампании

У «Деталей для АЭС / РСЯ» стоял набор **988806553** — тот самый старый,
который ведёт на `/prajs-list-detali/`, `/prajs-list-truby/`, `/uslugi/`
и `/trust-us/`. Все четыре спасаются редиректом, но несут метки roistat,
которого на новом сайте нет, а подпись «Сертификаты» открывает страницу
проектов.

Объявления пересозданы с набором **1509189628** (детали для АЭС, для ТЭС,
нормативная база, отводы по СТО плюс общие) и отображаемой ссылкой
`aes-tes` вместо кириллической. Старые остановлены.

Напоминание из граблей: `SitelinkSetId` и `AdExtensionIds` принимаются
только при создании объявления, поэтому смена набора ссылок — это всегда
пересоздание.

## 26. Запуск (10–11.09.2026)

Деньги пришли: баланс **14 648 ₽**. Модерация прошла **без единого
отклонения** — 71 объявление принято. Все 11 кампаний (семь новых и четыре
оставленные старые) пошли в показы сами: они стояли в `OFF`, а не
`SUSPENDED`, и после модерации и пополнения запустились без команды.

### Первые полтора дня

| Кампания | Показы | Клики | Расход |
|---|---:|---:|---:|
| Детали для АЭС / РСЯ | 254 | 12 | 40 ₽ |
| Детали для АЭС и ТЭС (поиск) | 29 | 3 | 54 ₽ |
| Трубы | 23 | 1 | 33 ₽ |
| Опоры | 12 | 1 | 3 ₽ |
| Тройники, Переходы, Отводы | 29 | 0 | 0 ₽ |
| Оптимизация ТГО | 1 | 0 | 0 ₽ |
| Стандарты ×2, Ретаргетинг по клиентам | 0 | 0 | 0 ₽ |

Метрика рекламный трафик видит: 5 визитов 10.09 и 16 визитов 11.09.

**Бюджет.** Недельный потолок по всем кампаниям — 37 000 ₽. При полной
выработке баланса хватило бы на три дня, но фактический темп пока ~90 ₽
в день: узкая семантика не выбирает лимиты.

### Автотаргетинг ведёт себя как задумано

86 поисковых запросов с показами, почти все — ровно наш покупатель:
«переход к 325х22 273х18 исп 2 ст 09г2с», «тройник п 57х6 ст 20 гост
17376», «отвод 45 76х5 ст 20 гост 17375», «опора скользящая ф720
хомутовая гост». Сужение до EXACT и ALTERNATIVE сработало.

Мусора нашлось два случая, оба — мои дефекты, оба исправлены:

* **«б у» не минусовалось.** В общем наборе было «бу», а раздельное
  написание — отдельное слово; прошёл «трубы б у большого диаметра».
  Раньше я выкинул «б у» из списка как дубль «бу» — ошибочно.
* **Фразы «изделие 20».** Генератор атомной кампании собрал «труба 20»,
  «отвод 20» и ещё пять из марки стали 20, но при широком соответствии
  «20» — это и диаметр 20 мм. Отсюда полимерная труба varmega PE-Xa в
  показах. Семь фраз остановлены, 26 однозначных «… сталь 20» оставлены,
  генератор больше таких не строит.

В общий стоп-лист добавлены «б у», varmega, evoh, pex — 51 слово.

### Почему молчат три кампании

Проверено: объявления приняты, группы допущены к показам, условие
ретаргетинга доступно. Дефектов нет.

* **«Стандарты» ×2** живут на номерах СТО и ОСТ — редкая семантика, за
  год она дала ~800 ₽ в неделю. Полтора дня без показов правдоподобны.
* **Ретаргетинг по клиентам** — аудитория 2 830 человек, за полтора дня
  до них реклама могла просто не дойти.
* **«Оптимизация ТГО»** — 1 показ. Это прямое следствие ставки 50 ₽
  вместо «наивысшей позиции», которая брала клики по 99–110 ₽. Кампания
  с худшим CPA аккаунта фактически остановилась сама; если объём по
  «купить трубу» нужен, потолок придётся поднять до 70–80 ₽.
  **11.09 по решению заказчика средняя цена поднята до 75 ₽**, недельный
  лимит оставлен 5 000 ₽ — это около 65 кликов в неделю при полной
  выработке. Через неделю сравнить CPA с прежними 9 832 ₽ по «Купить трубу».

### РСЯ: весь трафик — биржи на планшетах

19 площадок с показами. Все 12 кликов — с DSP-бирж (dsp.yandex.ru,
opera-exchange, inneractive, yeahmobi) на **планшетах**. Семь новых
приложений, которых нет в стоп-листе, показали рекламу, но кликов не дали.

Это тот самый трафик, у которого за год глубина визита была 1,0 страницы.
Мобильная корректировка −15% планшетов не касается. Для поиска планшеты
нормальны (CR 1,96%), для РСЯ — нет (CR 0,31%). Решение по корректировке
планшетов в сетевых кампаниях отложено до недели данных: полтора дня —
слишком мало, чтобы резать единственный источник её объёма.

## 27. Потолок 3 000 ₽ в сутки (14.09.2026)

Заказчик ограничил общий расход 3 000 ₽ в сутки и попросил распределить
его по эффективности. За 10–14.09 аккаунт тратил ~2 800 ₽ в сутки при
сумме недельных лимитов 37 000 ₽ — лимиты фактически не сдерживали.

**Дневной бюджет поставить нельзя.** API отвечает 6000 «Дневной бюджет
можно использовать только совместно с ручными стратегиями», а у всех
одиннадцати кампаний автостратегии. Потолок выражен недельными лимитами:
21 000 ₽ = 3 000 × 7. Это не жёсткий дневной потолок: в отдельный день
кампания может потратить больше седьмой части, но за неделю лимит не
превысит.

Основа распределения — данные 10–14.09: расход, клики и конверсии из
Директа, отказы и глубина из Метрики.

| Кампания | Было | Стало | Почему |
|---|---:|---:|---|
| Детали для АЭС и ТЭС (поиск) | 3 000 | **5 000** | единственная живая заявка + 5 автоконверсий, упиралась в лимит |
| Трубы | 2 000 | **3 500** | 5 автоконверсий, отказы 33% — лучшие в аккаунте |
| Отводы | 2 000 | **2 500** | глубина 1,30, упиралась в лимит |
| Тройники | 2 000 | 2 000 | 1 конверсия, глубина 1,35 |
| Опоры | 2 000 | 2 000 | 1 конверсия, глубина 1,35, отказы 42% |
| Переходы | 2 000 | **1 800** | конверсий нет, отказы 49% |
| Оптимизация ТГО | 5 000 | **1 500** | тест ставки 75 ₽ до 18.09, темп ~1 100 в неделю |
| Стандарты Россия | 5 000 | **400** | редкая семантика, тратит ~100 в неделю |
| Стандарты ДНР/ЛНР | 4 000 | **300** | то же, оставлена по решению заказчика |
| Ретаргетинг по клиентам | 5 000 | **1 000** | 0 конверсий при 1 852 ₽ |
| Детали для АЭС/РСЯ | 5 000 | **1 000** | 0 конверсий, отказы 67%, глубина 1,01 |
| **Итого** | 37 000 | **21 000** | |

Предыдущие стратегии сохранены в
`perf-reports/ads/2026-09-14/strategies-before-budget.json`. Пересмотр —
18.09, вместе с решением по ТГО: автоконверсии (12 из 14) ненадёжны,
и когда пойдут офлайн-конверсии из CRM, веса надо пересчитать по ним.

## 28. Атрибуция заявок (14.09.2026)

Первая за запуск живая заявка (№250033, лид 50787, 07:01 МСК) пришла
в Битрикс с пустыми `UF_CRM_YCLID` и `UF_CRM_METRIKA_CLIENT_ID`. Без них
офлайн-конверсию не к чему привязать.

**Причина — в сайте, а не у посетителя.** Метрика этот визит записала
(Edge, /contacts/, 08:55 и 09:01 по времени счётчика), то есть ClientID
в браузере был. В мете заявки в WordPress идентификаторов нет — их не
отправила форма. Форма страницы контактов `#cntForm` уходит обычным POST,
а `request-modal.js` дописывал скрытые поля только в подвальную
`#s10-form`. Так же без атрибуции уходили опросные листы всех категорий
(21 шаблон в `inc/category-content/`) и форма карточки товара. Работала
только модалка.

**Исправлено (коммит 4849a5f, выложено на прод):**

* скрипт дописывает поля во все формы с `action=promen_request` — при
  загрузке, после ответа `getClientID` и ещё раз в момент отправки;
* сервер при пустом POST берёт `_ym_uid` (это и есть ClientID) и
  `promen_yclid` из cookie — подстраховка на случай, если скрипт не
  отработал.

Проверено на проде: на /contacts/ и /catalog/opory/ (две формы) поля
заполнены, серверная функция отдаёт cookie при пустом POST и не даёт им
перебить значения из POST.

**Вторая дыра — в Директе.** У четырёх старых кампаний (ТГО, «Детали
РСЯ», оба «Стандарта») была выключена разметка ссылок для Метрики
(`ADD_METRICA_TAG = NO`): клики из них приходили без yclid. Включена через
API 14.09, у всех одиннадцати активных кампаний теперь `YES`.

**Лид 50787 восстановлен по Метрике.** Визит заявителя нашёлся по браузеру
и времени: ClientID `1789358119307148363`, Edge, 08:55 по времени счётчика,
**переход с рекламы** на /catalog/sdt/otvody/, 3 страницы, цель заявки
достигнута. Кампания в отчёте за текущий день ещё не проставлена —
уточнить 15.09. Когда лид перейдёт в «Качественный лид», конверсию
грузить по этому ClientID вручную. В сам лид в Битриксе ничего не
вписано — CRM правим только по команде заказчика.

## 29. Разбор первых пяти дней и чистка (14.09.2026)

За 10–14.09 реклама дала **одну веб-заявку** (250033, кампания «Детали
для АЭС и ТЭС», поиск), два клика по почте и **ноль звонков** по
коллтрекингу при ~1 500 кликах и ~11 800 ₽. Колонка «Конверсии» в Директе
(39, 31, 27…) — автоцели, а не заявки. Отказ по отдельному запросу — шум:
у запроса один клик, и отказ всегда 0 или 100%.

Куда уходили деньги:

* **фраза «детали трубопроводов»** в АЭС-поиске — 3 681 разный запрос,
  1 025 ₽ (40% кампании), отказы 68%: клипсы, евроконусы, футорки, хомуты;
* **автотаргетинг** — 67–100% расхода товарных кампаний (Трубы 100%,
  ТГО 93%, Переходы 83%), среди нужного — Rehau, чугун, вентиляция;
* **«Детали РСЯ»** — 355 из 383 кликов с планшетов через биржи, у Android
  Browser 90% отказов при 2 с;
* **ретаргетинг по клиентам** — 85% расхода на телефонах, мобильные игры
  и биржи, глубина ровно 1,00, заявок ноль;
* **телефоны в поиске** — отказы 56–73% против 24–47% с компьютеров.

**Применено по решению заказчика** (`scripts/ads/direct_cleanup_0914.py`,
состояние «до» — `perf-reports/ads/2026-09-14/cleanup-before.json`):

* в стоп-лист 76097012 добавлено 24 слова (51 → 75): полипропилен,
  пропиленовый, пвх, rehau, рехау, чугун, чугунный, вчшг, медь, медный,
  металлопластиковый, резиновый, пенополистирол, вентиляция, воздуховод,
  клипса, евроконус, футорка, сальник, подводка, елочка, дренаж, «как
  посчитать», «принцип действия». Каждое сверено с каталогом на проде —
  совпадений 0. **«Хомут» не добавлен**: в каталоге 12 опор хомутовых
  ОСТ 36-17-85;
* ТГО и «Стандартам» те же слова — в минус-слова кампаний (+17, остальные
  там уже были). Сам стоп-лист к ним **не подключён**: в нём «прайс», а у
  ТГО 99 фраз «… прайс», у «Стандартов» по 63 — они бы замолчали;
* ретаргетинг по клиентам 714267536 — **только компьютеры**: телефоны
  −100% (было −15%), планшеты −100% (корректировки не было). Сначала
  кампания была по ошибке остановлена: в пункте было два варианта, и я
  взял тот, за который высказался сам, а заказчик выбирал «только
  компьютеры». Через час включена обратно, корректировки поставлены до
  включения;
* мобильная корректировка Переходов и Тройников −15% → −30%;
* в обоих «Стандартах» остановлены (не удалены) «штуцеры трубопроводов
  -гост -купить», «производство пробок», «заглушки производство -атк…».

**Не применено:** остановка «детали трубопроводов» (сначала проверить,
не через неё ли пришла заявка 250033) и остановка «Деталей РСЯ». ТГО —
решение 18.09.

Сумма недельных лимитов по-прежнему 21 000 ₽. У ретаргетинга с одними
компьютерами объём упадёт: за 10–14.09 компьютеры дали 7 кликов и 146 ₽
из 1 876 ₽, так что лимит 1 000 ₽ он, скорее всего, не выберет.

Попутно: стоп-лист глушит две фразы новых кампаний с момента пересборки —
«крутоизогнутый отвод чертеж» (Отводы) и «переходы стальные гост 17378
2001 цена прайс» (Переходы). Денег не тратят, просто мёртвые.

Ноль звонков за пять дней — уточнить у менеджеров: если звонки с рекламы
были, коллтрекинг их не считает.

### Старые кампании: отказы и вторая чистка (14.09)

Отказы 50–100% в интерфейсе у ТГО, «Деталей РСЯ» и «Стандартов» — это
1–5 кликов за день. Но за ними три настоящие причины:

* **ТГО** — 28 из 30 кликов (650 из 700 ₽) дал автотаргетинг одной группы
  «Купить трубу», единственной в кампании с открытыми категориями
  «альтернативные», «конкуренты», «сопутствующие». Запросы справочные:
  сортамент, категории прочности, металлокалькулятор;
* **«Стандарты»** — оба сегодняшних клика на «штуцер» для сантехники;
* **«Детали РСЯ»** — 93% кликов с планшетов через биржи.

Применено (`scripts/ads/direct_cleanup_0914b.py`, «до» —
`perf-reports/ads/2026-09-14/cleanup-b-before.json`):

* ТГО, «Купить трубу» — автотаргетинг только по целевым запросам;
* ТГО — минус-слова сортамент, калькулятор, металлокалькулятор, прочность,
  тоннаж (в товарах и терминах каталога нет; есть страницы калькуляторов,
  но объявления ТГО туда не ведут);
* «Детали РСЯ» — планшеты −50% (телефоны там уже −50%).

**Не применено: автотаргетинг «Штуцеров» не выключить.** В ЕПК это
невозможно ни остановкой (8305), ни снятием всех категорий (5005), ни
режимом (есть только CATEGORIES и BRIEF). Он уже на минимуме — только
целевые, без брендов. Сантехнику режут минус-слова «подводка» и «елочка»,
добавленные днём; голый запрос «штуцер» отсечь нельзя — минус-слово
заглушит и фразы группы. Цена вопроса — 1 клик за пять дней.

## 30. Wordstat: пробелы семантики (14.09.2026)

Ключ Yandex Cloud Search API взят из проекта Арматурного завода
(`YANDEX_CLOUD_API_KEY`, `YANDEX_CLOUD_FOLDER_ID` в `.env`). Скрипт
`scripts/ads/wordstat_gaps.py`: 167 затравок «изделие × признак», 5 862
уникальных запроса, сверка по каждой кампании отдельно. Результат —
`perf-reports/ads/2026-09-14/wordstat-gaps.csv`, сырые ответы —
`wordstat-raw.json`.

Итог: покрыт 1 030, пробел ~1 660 (после отсева шума), нет в каталоге 104,
отрезан минусами кампании 113, общий стоп-лист 19, не наш ~2 930.

**Крупные пробелы, подтверждённые каталогом** (частота за 30 дней, широкая):

| Кандидат | Кампания | Частота | Каталог |
|---|---|---:|---|
| тройник гост 17376 (+ приварной, бесшовные) | Тройники | ~12 000 | ✓ фасет |
| заглушка гост 17379 (+ эллиптические) | АЭС поиск | ~9 000 | ✓ фасет |
| отводы/тройники/переходы/заглушки «ст 20» | по изделиям | ~13 000 | ✓ сталь 20 |
| отвод гост 30753 | Отводы | ~3 500 | ✓ фасет |
| производитель/завод отводов, тройников, переходов, днищ, заглушек | по изделиям | ~3 700 | ✓ |
| тройник/отвод/переходы/заглушки ост 34 | АЭС поиск | ~1 800 | ✓ 34-10-764, -699, -753 |
| опоры хомутовые, пружинные, «скользящие опоры купить» | Опоры | ~6 000 | ✓ группы |
| трубы гост 10704 / 8732 / 10705 / 3262 | Трубы | ~100 000 | ✓, но розница металлобаз; автотаргетинг уже ловит |

**Нет в каталоге, но спрос есть:** опоры ОСТ 36-146-88 (~15 000 —
А11, КП, КХ, ХБ), опоры серий 5.903-13 / 4.903-10 / ТС, трубы ГОСТ 8731
(~12 000). Если завод их делает — это вопрос каталога, не рекламы.

**Минус-слова с номерами своих ГОСТов:** «Отводы» — 17375, 22793, 34;
«АЭС поиск» — 17379. Это перекрёстная минусовка между группами: к фразам
с этим номером минус не применяется. Вредна только «17379» — фраз с ним
в кампании нет, и запросы по заглушкам ГОСТ 17379 не ловит никто.
Лечится добавлением фраз, минус можно оставить.

## 31. Инструменты

**`scripts/ads/direct_api.py`** — API Директа v5: кампании со стратегиями
и минус-словами, группы, ключи со ставками, объявления с посадочными,
отчёт по поисковым запросам с показами, CTR и расходом.

```bash
python scripts/ads/direct_api.py check
python scripts/ads/direct_api.py all --days 365 --out perf-reports/ads/2026-09-03
```

**`scripts/ads/offline_conversions.py`** — загрузка сделок в Метрику
как офлайн-конверсий по yclid и ClientID.

```bash
python scripts/ads/offline_conversions.py upload deals.csv --dry-run
python scripts/ads/offline_conversions.py status
```

**`scripts/ads/bitrix_probe.py`** — разведка Битрикс24: воронки, стадии,
пользовательские поля. Нужен `BITRIX_WEBHOOK` в `.env`.

**`scripts/ads/direct_audit.py`** — выгрузка рекламных срезов из Метрики.
Визитное пространство `ym:s:*` (посадочные, устройства, пол/возраст)
и рекламное `ym:ad:*` (клики, расход, конверсии) в одном JSON.

```bash
python scripts/ads/direct_audit.py --days 365 --out perf-reports/ads/2026-09-03
python scripts/ads/direct_audit.py --days 7 --only ad_campaigns,ad_platforms
```

Логин Директа берётся из `YANDEX_DIRECT_LOGIN` в `.env`; без него
расходные отчёты пропускаются, а Метрика отвечает 403.
